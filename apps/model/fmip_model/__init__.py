"""FMIP forecast model service (D-009).

Packages:

- ``training``: loaders that fill the training store (schema ``training``,
  D-016, D-028) from football-data.co.uk and Club Elo, recording every load
  with its source, terms and content hash.
"""

__version__ = "0.0.0"
