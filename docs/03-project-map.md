# Project map

**Purpose.** This file exists so that neither a human nor an agent has to scan
the repository to find anything. Read it first; open only what it points to.

**Maintenance rule.** Any PR that adds a file, removes a file, or moves a
responsibility must update this file in the same PR. A PR that does not is
incomplete.

**Status.** Phase 0. Entries marked *(planned)* do not exist yet.

---

## Top level

| Path | Purpose |
|---|---|
| `CLAUDE.md` | Operating rules for agents. Read before any work. |
| `README.md` | Human entry point, setup instructions. |
| `render.yaml` | The preview service (T-086, D-064). At the root because Render requires it there. Declares the free web service, its health check and every environment value except the database string, which is marked `sync: false` and never stored in git. |
| `docker-compose.yml` | Local dev stack. Postgres, Redis, the API, the web app and the model service (internal, no published port). `web` also takes `SITE_URL`, so a preview can tell the pages the name they are served under. The `preview` profile adds `tunnel` (`cloudflare/cloudflared`, T-085) — never started by a plain `docker compose up`. |
| `deploy/` | Production (T-074, D-048), and the public preview under `preview/` (T-086, D-064): `Dockerfile` builds the web app and the API into one image with the migrations and the seed, and carries the same `public` copy and assertion as the web image for the same reason; `start.mjs` supervises them, takes the container down if either half dies, and resolves `SITE_URL`/`WEB_BASE_URL` from `RENDER_EXTERNAL_URL` when neither is set -- the one host-specific name in the image, and it lives in the supervisor because that file is the deployment -- so the preview's links name an address that opens, and the log says which source was used. The service itself is `render.yaml` at the repository root -- one free web service, one secret asked for, one generated. Runbook: `docs/11-preview.md`. `docker-compose.prod.yml` (selected on the VPS by `COMPOSE_FILE` in `.env`): Caddy as the only listener, `web`/`api`/`model` unpublished, Postgres and Redis on `127.0.0.1`, a `migrate` tool service under the `tools` profile. `Caddyfile`: TLS with the Cloudflare origin certificate, `web` resolved through Docker DNS every second, dial retries, no buffering -- plus the one route that does not go to `web`, the exact path `/me/conversations/socket` to `api` (T-234), because a WebSocket upgrade cannot be proxied by a Next.js route handler and the browser must still see one origin. Two `handle` blocks, so the two are mutually exclusive. `rollout.sh`: build, migrate, then roll `model` → `api` → `web` one container at a time behind healthchecks. `verify-rollout.sh`: probes the site every 200 ms during a rollout and fails on any non-200 (the acceptance check). `check-setup.sh` with `report-capabilities.mjs`: one command that asks the running stack what this deployment can and cannot do -- container health, the public site through Caddy, and whether e-mail, push, a language model and ingestion are on, with the `.env` line to add for each one that is off (T-075). Anything optional being off is the designed state and never fails the run; what it catches is a credential filled in with its switch forgotten, which from outside looks exactly like having done nothing. The report prints no secret -- every key, password and connection string is reported as `set` or `empty` -- so its output can be pasted to whoever is helping. `certs/`: the origin certificate, git-ignored. Runbook: `docs/09-deploy.md`. |
| `scripts/` | Developer scripts. `check-dev-stack.sh` proves Postgres, Redis, `/health` and `/en` all answer. `dev-proxy.sh` (config in `dev-proxy/squid.conf`) runs a loopback-only forward proxy in Docker for a host that cannot reach the npm registry directly; see `06-session-handoff.md`, constraint 2. `public-preview.sh` is T-085 (`start` / `status` / `stop`): the development stack behind a Cloudflare quick tunnel, with `SITE_URL` and `WEB_BASE_URL` set to the address it is given and a check that `/en` answers through it. It also turns `DEMONSTRATION_DATA` on and refuses to start if the environment asks for anything else, because the seeded fixtures it is about to publish are only obviously invented to the person who loaded them; the marker is then verified on the served `/robots.txt` rather than assumed. Runbook `10-public-preview.md`. `backup/` is T-072: `backup.sh` (pg_dump in the container + manifest + off-provider copy through rclone + pruning), `restore-drill.sh` (restores into a throwaway Postgres and checks checksum, migrations, every row count, constraints), `fmip-backup.service`/`.timer` for the VPS. Runbook: `07-backups.md`. |
| `.dockerignore` | Keeps `node_modules`, build output and `.env` out of every image build context. |
| `package.json` | Workspace root. Pins the pnpm version and the `build`/`lint`/`typecheck`/`test`/`format` entry points. Checks run with `--continue`, so one run reports every broken workspace. |
| `turbo.json`, `pnpm-workspace.yaml` | Monorepo wiring. Task graph and workspace globs. **`globalEnv` is an allow-list**: a variable not named there does not reach a task, so a suite gated on it skips under `pnpm test` while passing when the package script is run directly. That is how T-231's fan-out test was skipped in CI for a whole PR. Add a variable here as well as to `.env.example` whenever a test depends on it. |
| `tsconfig.json` | Root TypeScript config for editors. Compiles nothing itself. |
| `prettier.config.mjs`, `.prettierignore` | Formatting. Re-exports `@fmip/config/prettier`; Markdown is excluded. |
| `.npmrc`, `.nvmrc`, `.editorconfig` | Toolchain pinning: pnpm resolution, Node 22, editor defaults. |
| `.env.example` | Every environment variable, documented. Keep in sync. |
| `.github/workflows/ci.yml` | CI. `E2E journeys` (T-080) starts Postgres, migrates, seeds, builds and starts the API and runs the journeys project against the web app. It has Redis and `NEXT_PUBLIC_CHAT_SOCKET_URL`, so the chat socket is reachable and real-time delivery is checked rather than assumed. `Verify` runs format, lint, typecheck, test and build against a real Postgres service: the migrations are applied and the seed loaded with the package scripts first, so the API's store tests run rather than skip. `E2E` runs the Playwright RTL check. Both on every PR and on `main`. |
| `docs/` | All project documentation. See below. |
| `apps/` | Deployable applications. |
| `packages/` | Shared libraries. |

## `docs/`

| File | Contains | Read it when |
|---|---|---|
| `00-decisions.md` | Every locked decision with rationale | Before proposing any architectural change |
| `01-roadmap.md` | Phases and sequencing rule | Deciding whether something is in scope |
| `02-architecture.md` | Module boundaries, data flow, adapters | Starting work in a new module |
| `03-project-map.md` | This file | Always, first |
| `04-tasks-phase-1.md` | Task backlog with acceptance criteria | Picking up work |
| `04-tasks-phase-2.md` | The Phase 2 backlog: epics, the order they unblock each other in, and which two-thirds need nobody's permission | Planning or picking up Phase 2 |
| `04-tasks-phase-3.md` | The Phase 3 backlog: the community epics in the order the exits come before the surfaces (D-053), and what the maintainer has to write rather than the agent | Planning or picking up Phase 3 |
| `04-tasks-phase-4.md` | The Phase 4 backlog: the seven remaining languages, watch and highlights, native mobile, and notifications that leave the building. Written on request while Phase 3 is still being built, so every epic separates what is buildable before its blocker clears from what is not -- eight of eighteen tasks need nothing from the maintainer. Records what the phase declines to build. | Planning Phase 4, or checking whether something is blocked |
| `05-data-providers.md` | Provider research, the bake-off protocol, and which free source the pipeline runs on until T-025 (D-049) | Touching ingestion |
| `06-session-handoff.md` | How to resume in a fresh chat | Starting a new session |
| `07-backups.md` | Backups and the restore drill (T-072, D-032) | Touching the database host or the backup scripts |
| `08-load-test.md` | The live-path load test: threshold, tool, record of every run, what limits it, when to rerun (T-073, D-047) | Touching the stream, or before a big match or a deploy |
| `09-deploy.md` | The production runbook: server, Cloudflare, origin certificate, first start, zero-downtime redeploy, laptop rehearsal, record (T-074, D-048) | Deploying, redeploying, or touching `deploy/` |
| `10-public-preview.md` | A free public address for testing before the deploy exists: the quick tunnel, what it does not carry, and the ranked options for a stable one (T-085, D-050) | Showing the site to a phone or a person, or choosing where to host it |
| `11-preview.md` | The stable free preview on Render with a Neon database: the one-container shape and why, the three things the maintainer does by hand, and what each line of the log means when something is wrong (T-086, D-064) | Deploying or debugging the preview |
| `12-power-index.md` | The Power Index: where each piece lives, the two rules it is built on, and the weight validation with the record of every run (T-110..T-114) | Touching the index, its weights or its panel |
| `13-policy.md` | The Phase 3 policy (D-059): the contributor thresholds, the conduct ladder a moderator is shown, how a grant ends, and the platform rules and the contributor rules a member accepts, approved 2026-09-15 | Touching eligibility, moderation defaults, or anything a member accepts |
| `14-maintainer.md` | The things no agent session can do -- account creation, money, secrets, a physical device, a judgement -- in the order they unblock work, with the commands and how to know each worked | Asking what is left for the maintainer, or finishing one of them |
| `product-blueprint.md` | The original product definition, converted from `m1.docx`. Authoritative on behaviour, **not** on engineering | Questions about intended behaviour |
| `adr/` *(planned)* | Long-form decision records when a log entry is not enough | — |

## `apps/`

| Path | Purpose | Talks to |
|---|---|---|
| `apps/web` | Next.js App Router application. Locale routing, Tailwind, RTL-safe by lint. | `apps/api` over HTTP + SSE; `packages/contracts` for types |
| `apps/api` | NestJS backend on the Fastify adapter. One module per boundary. | Postgres, Redis, `apps/model` |
| `apps/model` | Python forecast model service (D-009): training-store loaders (T-060), the Dixon-Coles model (T-061), the backtest harness (T-062) and the FastAPI service (T-063). | Reads and writes the `training` schema; will be called by `apps/api` only. |

### `apps/api`

| Path | Purpose |
|---|---|
| `scripts/power-index-backtest.mjs` | The weight validation (T-113): walks a division's season, measures every match from only what came before it, scores each candidate weight set by held-out log-loss against the season's own outcome frequencies, and rewrites the table in `docs/12-power-index.md`. Needs a loaded training store, so it is run by hand. |
| `scripts/load-sse.mjs` | The live-path load test (T-073, D-047): N stream clients, a temporary live fixture changed through the database, one JSON report of connect, propagation, heartbeats and drops. Runbook and record: `docs/08-load-test.md`. |
| `src/main.ts` | Bootstrap: Fastify adapter with request ids, the JSON logger, the access log and the exception filter (T-071), port resolution, shutdown hooks. |
| `src/observability/` | Structured logging, request ids and error tracking (T-071, D-044): `json-logger.ts` (one JSON object per line, pretty in development, `LOG_FORMAT`; `json-logger.spec.ts`), `http-observability.ts` (`requestIdFrom`, the access log hook, `AllExceptionsFilter` — a 500 carries the request id, the stack stays in the log; `http-observability.http.spec.ts`). |
| — | `pnpm dev` watches and compiles; `pnpm dev:serve` runs the output. Two scripts, not one backgrounded pipeline, because `&` backgrounds in bash and sequences in cmd. |
| `src/app.module.ts` | Root module. Each boundary is registered here as it is built. |
| `src/database/database.module.ts` | The shared `pg.Pool`, injected as `PG_POOL` (D-025). Global, so boundary modules do not import it. Refuses to boot without `DATABASE_URL`; drains the pool on shutdown. |
| `src/modules/founder/` | The founder's analysis boundary (blueprint 6.5, T-131), separate from the forecast boundary on purpose (rule 6): `founder.controller.ts` is `GET /fixtures/:id/founder-analysis` (public) and `POST` (the `founder` role, not `admin`); `founder.service.ts` turns the database's kick-off refusal (SQLSTATE `PL002`) into a 409; `internal/founder-store.ts` publishes the version and writes its audit row in one transaction; `internal/validate.ts` names every bad field at once. `founder-feed.controller.ts` is `GET /founder-analyses?limit=&team=&competition=` (T-132), the one endpoint behind the homepage, team and competition feeds: upcoming matches only, newest version, an excerpt rather than the whole analysis, and a bad filter ignored rather than turned into a 400. `founder.spec.ts` covers the rules, `founder.http.spec.ts` the gate, the versions, the audit rows, the wall and the feed. |
| `src/modules/health/` | `GET /health`. Liveness only — see the note below. Returns `HealthReport` from `@fmip/contracts`. The ingestion and live views (`GET /health/ingestion`, `GET /health/live`, T-071) live in the boundaries that own the data, not here. |
| `src/modules/admin/` | The administration boundary (T-070, D-046), `admin` role only: `admin.controller.ts` — `GET /admin/overview` (coverage per current season, freshness, ingestion health, the rating rules in force), `GET /admin/users?q=`, `POST /admin/users/:id/status`, `PUT /admin/coverage/:seasonId/:module` (each with a reason and an audit row in the same transaction), `GET /admin/audit`; `admin.service.ts`; `internal/admin-store.ts` is the SQL. `admin.http.spec.ts` proves the gate and the audit records. |
| `src/modules/catalog/` | The catalog boundary's read side. `GET /countries`, `GET /teams` and `GET /competitions` as summary lists for forms and follow controls. T-035: `GET /competitions/:id?season=` — `CatalogService.competition` (season chosen by `pickSeason`: requested, else current, else newest), `internal/competition-store.ts` (competition with country, seasons, stages, every fixture of the season with teams and score, declared coverage); the table and the leaders come from the standings boundary. `competition.http.spec.ts` builds a league with two seasons and proves the selector and the coverage states. T-036: `GET /teams/:id` — `CatalogService.team` (competitions the team is active in with `tableContext`, the team's slice of the standings table; next and previous match; fixtures and results; squad from open spells via `derived`; follower count), `internal/team-store.ts`; `catalog.spec.ts` covers `pickSeason` and `tableContext`, `team.http.spec.ts` the page against the schema. T-037: `GET /players/:id` — `CatalogService.player` (identity, spells, a per-season record and the recent-match log derived from line-ups and incidents), `internal/player-store.ts`; `player.http.spec.ts`. |
| `src/modules/search/` | The search boundary (T-038, D-039): `search.service.ts` is public (`SearchService.search`, `parseSearchQuery`, the limits); `search.controller.ts` is `GET /search?q=&types=&limit=`; `internal/search-query.ts` parses and names every bad field (`search.spec.ts`); `internal/search-store.ts` is the SQL — `search_key()` on both sides, trigram word similarity or prefix, exact short name or code, aliases resolved to their entity, one row per entity. `search.http.spec.ts` proves aliases, misspellings, transliterations and accent folding. |
| `src/modules/news/` | News (blueprint 3.3, E14) under the rights each source grants (D-061). `news-ingestion.service.ts` reads every active `news_source` through `readFeed` from `@fmip/ingestion`, obeys the publisher's `robots.txt` (`internal/robots.ts`), applies the source's rights before the write, and writes a version only when the feed's words changed; every attempt is a `news_fetch` row. `news-clustering.service.ts` says where a report belongs: `internal/news-store.ts` links the teams and competitions a headline names by a whole-word match of their own name or alias (never a person, never a name under five letters) and the one fixture between the linked teams within two days, then merges a first-seen report into another publisher's story only when the two link the same teams and their headlines still read alike (trigram >= 0.4) once every linked name is removed -- the constants and the calibration are in the service. `story.promoted_article_id` is the earliest published report. `news.controller.ts` is `GET /news?section=` (T-143): latest, trending, debate and following over `internal/news-read-store.ts`, whose one `story_card` -- the promoted original, from a source that still grants it, only what the rights allow -- every section joins and orders differently; the `country`, `competition`, `team` and `language` filters apply to the whole cluster. Trending ranks by distinct members on the public panel of the story's matches inside 48 h and is `limited` with reason `discussion_only`; debate joins `story_debate`; following asks `ProfileService.listFollowing`, and for a guest is `not_supplied` with `needs_session`. `GET /news/stories/:id` (T-144) is the story page: the original in `?language=` when it has one, its languages, corrections, the other publishers' reports, and a body that is `not_supplied` unless the source grants `full_text`. `translations-admin.controller.ts` (T-304) is `POST /admin/articles/:id/translations` (a person's version in another language, as a new version) and `POST .../translations/:language/review` (a second person's approval, as another new version; the author is refused), over `internal/translations-admin-store.ts`, each write an `audit_log` row. `debate-admin.controller.ts` is the editor's half (`editor` or `admin`): `GET /admin/debates`, `POST /admin/stories/:id/debate` with the note readers see, `POST .../debate/clear` with a reason; `internal/debate-admin-store.ts` writes each decision with its `audit_log` row (target type `story`) in one transaction. `news-scheduler.service.ts` is the hourly BullMQ job behind the same `INGESTION_SCHEDULE=on` gate as the football jobs, on its own queue. `internal/news-transport.ts` is the one place the module reaches the network, named in the user agent so a publisher can refuse it. Imports nothing from the football boundaries: news is not on the critical path (rule 9). T-145 adds `GET /fixtures/:id/news`: the same cards for stories linked to a match or either side inside the window around kick-off, `not_supplied` until the feeds have been read at all. |
| `src/modules/standings/` | The standings boundary (T-035, D-038): `standings.service.ts` is public (`StandingsService.table(seasonId)`, `.leaders(seasonId)`, both `Covered` under the season's declared coverage; re-exports the pure `rankTable`); `internal/table.ts` is the ranking (points, goal difference, goals scored, name; form of five) — `table.spec.ts`; `internal/standings-store.ts` reads finished league-stage results, participants and goal incidents. No controller: the competition page reads it. |
| `src/modules/profile/` | The profile boundary: `GET /profiles/:username` (privacy applied to the viewer before anything is serialised), `GET`/`PATCH /me/profile`, `PATCH /me/privacy`, and the viewing territory (T-312): `GET /territories`, `GET`/`PUT /me/territory` (a code in any case, or `null` to clear; an unknown code is a 400 from the foreign key, never a neighbour), with `OwnProfile.viewing_territory` carried for the settings page. `internal/visibility.ts` is the one rule (`canView`) plus the `FriendshipOracle` port, bound by `ProfileModule` to `SocialService.areFriends` (T-201) so the `friends` visibility means what the settings page says. `profile.http.spec.ts` proves public / friends / private server-side. `ProfileService.predictionHistoryAccess` (T-056) applies the same rule to `prediction_history_visibility` for the predictions boundary. Following (T-042): `GET`/`PUT`/`DELETE /me/following[/:type/:id]`, `GET /me/favourites`; `internal/following-store.ts` is the SQL; `internal/favourite-order.ts` is `compareByFavourites`, the pure ranking the scores API sorts with (exported from `profile.service.ts`). |
| `src/modules/viewing/` | Watch and highlights (blueprint 11, E31): `GET /fixtures/:id/viewing` and `GET /viewing?fixture=…` answer `MatchViewing` for the viewer's stored territory or `?territory=` (an unknown code refused, none answered as `not_chosen`); the editorial desk's endpoints under `/admin` (T-313, D-069) declare coverage per season, territory and module, keep the broadcaster list, and enter, replace and remove listings and official highlight pages, coverage first and every write audited (rule 10). Not on the critical path (rule 9); imports identity and profile only. |
| `src/modules/conversations/` | The conversations boundary (blueprint 8.3, Phase 3). T-220 is the schema only: `conversations.schema.spec.ts` covers what the database guarantees -- numbering from one per conversation, a caller-supplied sequence overwritten, a refused message spending no number, `PL006` for a non-participant or somebody who has left, `PL003` across a block, `PL004` under a messaging sanction, and `PL007` on any change to a message but the tombstone. `conversations.service.ts` is the public surface and `internal/conversations-store.ts` every statement; `conversations.controller.ts` is `/me/conversations` -- list, `POST direct/:username` (idempotent, and **needs a friendship**: blueprint 8.1 gives starting a direct conversation to friends, which is what removes the DM-spam surface), read a page (`?before=<seq>`), send, remove your own message, read state, mute and leave. An existing conversation stays writable after a friendship ends -- unfriending is not blocking. A conversation the viewer is not in is 404 rather than 403. `conversations.http.spec.ts` proves the block, the sanction and the leave. T-222: a message may carry one football card as `card_kind` + `card_id` and nothing else; `CardStore` resolves it at read time, one query per kind, so a shared match stays live and carries its own `last_updated_at`. A member shares their own prediction and nobody else's; a removed message keeps no card; an entity that no longer resolves comes back as `gone`. T-223: `GET :id/search?q=` over `search_key(body)` and a partial trigram index -- not a text-search configuration, because stemming needs a language and picking one would search well in English and badly in seven others, silently. A removed message is never a result, and searching works after leaving. T-225: reactions, mentions and pins -- `PUT`/`DELETE .../reactions/:reaction` and `POST`/`DELETE .../pin`; a mention names only members already in the conversation, a refused mention is dropped rather than failing the message, and `ConversationPage.pinned` is sent whatever page is being read. T-230: `chat.gateway.ts` is the same boundary over a socket -- one `ws` server with `noServer: true` on the Node server Fastify already listens on, answering `upgrade` at `/me/conversations/socket` (D-055). It **delivers and never decides**: `subscribe` and `unsubscribe` are the only frames a client may send, and every write stays on the controller above. Authorisation happens at subscribe and again at each delivery, and the delivery check asks a second question -- `participation()` still returns a row after a member leaves so that history stays readable (T-223), which is not the same as being owed live messages. A `subscribe` from somebody who has left is refused `not_a_participant` rather than `not_found`, because they can read the conversation and would know. The `Origin` allow-list is the whole defence against cross-site socket hijacking (a WebSocket handshake ignores CORS), and the heartbeat that hangs up on a dead socket also re-authenticates it, so logging out closes the channel. T-231: `internal/chat-bus.ts` is what publishes into it -- Redis pub/sub on one channel (`fmip:chat`), with the two connections Redis requires, so a message written on one instance reaches a socket held by another. Every instance hears every broadcast and drops what no local socket asked for; per-conversation channels would need a subscription table kept in step with the socket registry, which is a second source of truth for who is listening (D-056). A publish that fails is logged and dropped rather than failing a send that already succeeded, and with no `REDIS_URL` the bus is `AbsentChatBus`, which says so once and reports `healthy: false` instead of accepting broadcasts and delivering nothing. `chat-fanout.spec.ts` runs **two** whole API instances against one Postgres and one Redis, because a single-instance test would pass with an in-process fan-out and prove nothing.  T-235: `subscribe` may carry `after_seq`, and the answer is a `catch_up` frame -- at most one page (`ConversationsService.since`, `ConversationsStore.after`), oldest first, `more` when the gap was longer. The subscription is registered **before** the history is read, so the seam can overlap but never open: a message written in between arrives twice rather than never, and the client drops the duplicate by `seq`. An empty `catch_up` is still sent, because no frame and an empty frame look the same to a client that is waiting.  T-233: `chat-health.controller.ts` is `GET /health/chat` -- public and read-only like `/health/live` beside it, and naming nobody: bus state (`connected` / `down` / `absent`, three states because "never configured" and "broke" are different problems), open connections, held subscriptions, events delivered, subscriptions dropped at delivery, handshakes refused by reason, and delivery latency as p50/p95 over the last hundred events, measured against the publishing instance's clock. Every number is **this instance**, and `instance` (a boot id) is how two answers are told apart; summing the fleet is the scraper's job, not an endpoint's.  T-232: when a shared fixture moves, the card is sent again as a `card` event naming the message it hangs on, so a client replaces it in place and the conversation does not move. It does **not** go through Redis: Postgres already announces a fixture change to every instance (T-032, D-034), so each refreshes its own sockets -- publishing it on the chat bus as well would deliver every update once per instance. A burst is collapsed per fixture, a change nobody is watching costs no query, and the gateway takes "a fixture moved" through its own `FIXTURE_CHANGES` port, bound in `conversations.module.ts` to the feed the fixtures boundary exports. |
| `src/modules/groups/` | The groups boundary (blueprint 8.2 and the exclusive groups of 10.1, Phase 3). T-240 is the schema: `groups.schema.spec.ts` covers what the database guarantees -- three visibilities and no fourth, a slug that cannot be renamed, exactly one owner with ownership handed over inside one transaction, a group refused at commit if it never gets an owner, an invitation refused across a block or under a contact sanction or to somebody already inside, a join request refused for the two visibilities that have no door to knock on, a `groups` sanction that stops making and joining but not reading or leaving, and a ceiling on how many groups an hour holds. T-241 is the API: `groups.service.ts` is the public surface and `internal/groups-store.ts` every statement, with `groups.controller.ts` at `/groups` and `/me/group-*`. **It decides who is asking and nothing about who may be where** -- every rule is the schema's, and the service turns the refusal into a sentence and a status code. Two doors rather than one endpoint that means two things: `POST :slug/members` joins a public group, `POST :slug/requests` asks a discoverable one, and `standing` says which. An invite-only group is **404 on every verb**, never 403, except to somebody holding an invitation -- an invitation is itself being told it exists. A discoverable group answers 200 with `members: null`, never `[]`, because an empty array would say nobody is in it. Inviting needs a verified e-mail, the same gate a friend request passes. T-245: a group's conversation, made in the same transaction as the group. **Its membership is the group's, not a copy of it** (D-058) -- `refuse_non_participant()` and `ConversationsStore.participation()` both branch on the conversation's kind, and that one query is where the page, the search, the catch-up and both halves of the socket ask, so a membership change is immediate on all of them with nothing to synchronise. `conversation_participant` keeps the read position and the mute and no authority; a group member has no row until they read or mute, and its absence means "has read nothing", never "is not here". Leaving a group conversation is refused with "leave the group instead", because a `left_at` there would change nothing and report success. T-243: `GroupsService.audience(viewerId, slug)` answers who is in a group and whether this viewer may ask -- a narrower outcome than `GroupOutcome` because the question has only two refusals, and `members_only` is not `forbidden`: one is for the people who *run* the group, the other for the people who are *in* it. This boundary ranks nobody; the board is the reputation module's (D-060). |
| `src/modules/moderation/` | The moderation boundary (blueprint 10.4, D-053). `moderation.service.ts` is the public surface — `report`, `ownReports`, `standing`, `activeSanction`, `appeal` — and `internal/moderation-store.ts` every statement; `moderation.controller.ts` is the **member's** half: `POST /reports`, `GET /me/reports`, `GET /me/standing`, `POST`/`GET /me/sanctions/:id/appeal`. Reporting needs a session and nothing else: reaching another member is gated, getting help about one never is. `activeSanction` is read by the social boundary *after* the database has refused a write (PL004), to explain it rather than to decide it. `moderation-admin.controller.ts` is the moderator's half (T-212), `moderator` or `admin`: `GET /admin/moderation/queue` (open reports grouped by subject, oldest waiter first, with the sanctions already in force), `GET /admin/moderation/members/:username` (their reports, decisions and sanctions), `POST /admin/moderation/decisions` and `POST /admin/moderation/sanctions/:id/lift`. `internal/moderation-store.ts`'s `ModerationQueueStore.decide` writes the decision, the reports it answers, any sanction and the `audit_log` row in one transaction (D-046). An outcome of `sanctioned` must carry a restriction and no other outcome may. `moderation.schema.spec.ts` (T-210) covers what the database guarantees — an immutable decision, a sanction that must end or say it is permanent, a lift that is whole or absent, `member_sanctioned` over time, `PL004`, one open report per subject; `moderation.http.spec.ts` (T-211) covers the surface, including a sanctioned member refused a friend request and still able to file a report. |
| `src/modules/analysis/` | Community-written match analysis (blueprint 10.3, E26). **Its own boundary, and that is the epic's whole argument**: the obvious alternative is a second author on the founder's analysis, and it would make the founder's signature mean nothing (rule 6). It shares no table, no type and no service with `FounderModule`, and the two never import each other. `analysis.service.ts` is the workflow (`saveDraft`, `workspace`, `submit`, `queue`, `decide`, `published`) and **enforces nothing the schema already does**: `PL014` for the grant, `PL002` for kick-off, a primary key for "one decision per submission" -- it turns each refusal into a sentence and adds none of its own. `internal/analysis-store.ts` is every statement; `decide` records the review, publishes when it is an approval and writes the `audit_log` row in **one transaction**, because a reviewer saying yes is saying it may be read and a decision that recorded an approval then failed to publish would leave an analyst told yes and a public told nothing. `analysis.controller.ts` has three audiences by route prefix: `GET /fixtures/:id/community-analyses` is public and asks for no session, `/me/analyses/*` is the analyst's workspace, and `/admin/analysis-reviews/*` needs **`editor` or `admin`**. `editor` is a new role (`..._editor-role.sql`, T-261) and deliberately not `moderator`: moderation is about conduct, editorial review is about whether writing is good enough to publish under the platform's name, and one role for both would make every moderator an editor by accident. A decision needs a reason even when it is an approval, and the analyst is notified either way. T-262's surfaces are `apps/web/src/components/analysis-editor.tsx` and `analysis-queue.tsx`. The editor puts **the decision history on the same page as the draft** -- a request for changes shown on its own is an instruction with no context, and the analyst would be rewriting from memory -- and names each bad field beside its own input rather than in one lump. The queue shows the author **linked to their record**, says which attempt this is when it is not the first (the second time somebody submits the same thing is a different situation), and requires a reason on every decision including an approval. Neither checks a grant, a role or a kick-off: all three live in the database and are worded by the API, and the copy in the browser is the one that goes stale first. "You may not read this" and "nothing is waiting" are rendered as different sentences. T-263 adds `community-analysis-panel.tsx` to the match page, below the founder's analysis and **visibly not it**: the heading names who is speaking and a line under it says what this is not. `community-analysis-panel.spec.ts` is the four-way guard where a reader actually meets the rule -- the failure it catches is not a merged type but one component that renders "an analysis" whichever it was given, and that would pass every assertion in `three-products.spec.ts`. Each analysis carries its author's name and rating (blueprint 10.3), says when it was revised, and shows a formerly approved analyst as former while keeping their work: taking it down would rewrite the record and calling them approved would be false. |
| `src/modules/following-feed/` | The Following feed (blueprint 12.1, T-333): `GET /me/feed` over `internal/following-feed-store.ts` (matches, stories, founder analyses and followed contributors' posts, each bounded by the window and by what the member follows) and `following-feed.service.ts`, whose `rank()` is `feed-rank@1` -- favourite 2, live 3, imminent 2, discussed 1-2 for distinct members, fresh 1; a follow is inclusion -- with every item carrying its `because` and `rank`. Imports identity and profile; reads the rest as tables. |
| `src/modules/delivery/` | Delivery behind one port (T-330): `delivery.port.ts` (`OUTBOUND_DELIVERY`, an e-mail and a push channel each present with a provider or absent, `deliveryFromEnv` reading `DELIVERY_EMAIL_PROVIDER`/`DELIVERY_PUSH_PROVIDER` and refusing to start on a name this build cannot drive), `delivery.service.ts` (`deliver()` on every channel that exists, `absent`/`sent`/`failed` per channel, the state logged at boot) and `GET /health/delivery`. Imports nothing, so notifications can import it. D-073 (2026-09-20): `internal/smtp-email.ts` is the e-mail channel -- `smtp` in `KNOWN_EMAIL_PROVIDERS`, `SMTP_URL` + `DELIVERY_EMAIL_FROM` required, `nodemailer` underneath, nothing connected until the first send. D-074 (2026-09-20): `internal/webpush.ts` is the push channel (`webpush`, VAPID keys, `web-push` underneath; a device gone at its service is removed; no device is `NoRecipient`, recorded `skipped`), `internal/push-subscriptions.ts` the member's devices (`push_subscription`), and `DeliveryService.pushState/registerDevice/removeDevice` what the settings page needs. |
| `src/modules/intelligence/` | The intelligence layer behind one port (Phase 5, D-070, T-401): `intelligence.port.ts` (`LANGUAGE_MODEL`, `AbsentIntelligence`, `settingsFromEnv` -- `off` is an honest absence; an unknown provider, a driveable one with no key, or a bad effort refuses to start), `intelligence.service.ts` (`complete()` → absent / completed / failed, never an exception; logs the state once at boot), `GET /health/intelligence`, and `internal/anthropic-model.ts` (T-402: the Messages API through the official SDK, adaptive thinking, the deployment's effort, the refusal fallback; a refusal or a truncation is a stop reason, not text). Nothing on the critical path imports it. T-404 (D-072): `internal/chat-completions-model.ts` is the second adapter, the `POST /chat/completions` shape over `fetch` with no dependency -- `mistral` as a named preset (`MISTRAL_API_KEY`, `mistral-small-latest`, the effort as `reasoning_effort`) and `openai_compatible` for any endpoint named by `INTELLIGENCE_BASE_URL`, `INTELLIGENCE_API_KEY` and `INTELLIGENCE_MODEL`; the finish reason is read before the text, a 429 is retried once after the pause the server names, and `settingsFromEnv` refuses the generic name without its URL or its model. |
| `src/modules/summaries/` | Match summaries (E41, Phase 5): `internal/match-facts.ts` (T-410: the record as facts, each part under its coverage, absences named, forecast and consensus as labelled numbers), `internal/grounding.ts` (T-411: the gate -- every number and every capitalised name in the text must be in the facts), `internal/match-summary-store.ts` (immutable versions with the facts, model, prompt version and the editor's audit row), `summaries.service.ts` (T-412: assemble, ask the port, gate, store; refusal, truncation and a failed gate are rejected versions; a ten-minute catch-up when a model exists), `GET /fixtures/:id/summary` and `POST /admin/fixtures/:id/summary`. Reads fixtures, forecast and consensus through their public services; nothing imports it. T-412's thin record (2026-09-19): `thinRecord()` in `internal/match-facts.ts` -- no timeline, statistics or line-ups held -- and `generate()` writes a `skipped` version with the reason instead of asking (`..._match-summary-skipped.sql` widens the state; `latestState()` reads it), so the page's reason is `thin_record` and the catch-up does not ask again. |
| `src/modules/ask/` | Natural-language search (E42): `GET /ask?q=` -- the model reads the question into `SearchIntent` (names and kinds, `readIntent` strict about the schema), the search module's public service answers by id, and every failed reading (no model, unreadable, nothing named, a failed call) is the question searched as keywords with the reason. |
| `src/modules/moderation-assist/` | Moderation assistance (E44): `moderation_suggestion` versions beside a report (a category from the four reasons plus `no_action`, the reasoning, the model, the prompt version; never a decision), `ModerationAssistService` (the rules in the standing instruction, the report's reason and the reporter's words as the whole prompt, a strict reading, a five-minute catch-up when a model exists), `POST /admin/moderation/reports/:id/suggest`; the moderation queue carries the latest suggestion and the assistant's state. |
| `src/modules/briefings/` | Briefings (E43): `internal/briefing-document.ts` (T-430: the feed's window grouped by day, each item by id; T-431's gate holding every paragraph and every number to the document), `member_briefing` versions, `GET /me/briefing` and `POST /me/briefing` (written when the member asks, never on a page load). Reads the feed through its public service. T-432: a published briefing is emitted as the inbox's own `briefing` kind (subject: the `member_briefing` row; dedupe key: the date the window starts on, so once a day however many versions), the outcome (`sent`, `delayed`, `duplicate`, `muted`, `failed`) is in the `POST` response, and `carry()` hands the notifications module a composer -- the prose by e-mail, its first paragraph as a push opening `/following#briefing` -- at once for a request and every five minutes for ones quiet hours held. |
| `src/modules/campaigns/` | Campaigns (T-332, D-075): `internal/campaign-store.ts` (audiences as saved filters with a closed vocabulary and a size, campaigns, the dispatch claimed once by primary key, a send row per member, the tally, the audit rows), `campaigns.service.ts` (`send()` tells every active member the filter reaches through the inbox's own `emit()`, so preference, quiet hours and mutes apply; registers the `campaign` composer -- title and body, opening the path; carries the reached members at once), `campaigns.controller.ts` (administrators only: `GET`/`POST /admin/audiences`, `GET`/`POST /admin/campaigns`, `GET /admin/campaigns/:id`, `POST /admin/campaigns/:id/send`, 409 on a second send). `..._campaigns.sql` adds the tables and the `campaign` kind and subject; the inbox and the carrier resolve a campaign's path as `subject_label` and its title as `headline`. |
| `src/modules/notifications/` | In-product notifications (blueprint 12.2, E27). `GET /me/notifications` carries `delivery` (T-330) so the inbox can say when it is the only place a notification exists. T-331 adds the mutes: `GET /me/notification-settings` carries `mutes`, `PUT`/`DELETE /me/notification-mutes/:scope/:target` silence and unsilence a team, a competition or a category, `wants()` reads a category mute beside the kind preference, and `emit()` asks `notification_muted_for()` about the subject before writing. **It imports nothing**, and that is what makes it safe for every other module to import: a notification is a consequence of something that happened elsewhere, so the boundary that records consequences must not depend on the ones that produce them -- a cycle here would be a cycle between almost every module in the product. The cost is that it knows nothing about what it is describing: ids and kinds, not fixtures and conversations, and the deep link is resolved by whoever reads the inbox. `notifications.service.ts` is `emit`, `emitMany` and `wants`; `internal/notifications-store.ts` is every statement. **`emit` never throws.** A friend request that succeeded and then failed because a notification could not be written is one the member is told failed, and they will send it again -- so the outcome is a value (`sent`, `muted`, `duplicate`, `blocked`, `failed`) and a real fault is logged and swallowed. That is a deliberate asymmetry with the rest of the codebase and is safe only because nothing depends on a notification existing. The block is **not** checked here: `notification_block_guard` refuses one whose source the recipient blocked and this treats `PL003` as an ordinary outcome, because a check in the service would go stale the moment a block was created between the check and the write. The defaults are overlaid in `wants` from `NOTIFICATION_DEFAULTS`, so changing one reaches everybody who never chose. T-271's first producer is the social graph: `social.service.ts` emits `friend_request` and `friend_accepted`, **only when the write actually changed something** -- re-sending a request that already stands changes nothing, and telling somebody again about something that has not changed is the "emitted twice" the task is measured by. `emission.http.spec.ts` covers both negatives, including a block in the direction a check written from the blocker's side would miss. The other producers each emit from an event that already happened: `groups.service.ts` sends `group_invite` to the invitee (keyed on the group, so withdrawn-and-re-sent is the same group asking the same person) and `group_join_request` to the **deciders only** -- `GroupsStore.deciderIds` asks for owner and moderator, because a group of two hundred told about a queue two of them can act on is how a member turns notifications off entirely; `moderation.service.ts` tells a member what was decided about them **with no source**, because policy section 2 promises *which* decision and *why*, not who made it, and naming the moderator would hand a sanctioned member somebody to blame (the audit row names them, where it is read by people who can be held responsible for reading it); `contributor.service.ts` tells a member their grant was given or changed, also sourceless and for the same reason, keyed per act rather than per grant so a pause and a later withdrawal are two things they hear about; `panel-social.service.ts` tells a contributor somebody reacted, on the way in only and once per reactor per post -- they are told somebody engaged with what they wrote, not counted at, and the kind is off by default. T-272 adds `notifications.controller.ts` -- **everything under `/me`**, because an inbox is the closest thing this product has to somebody's private correspondence with it, and there is no reading of anybody else's for an administrator either: `GET /me/notifications` (unread counted across the whole inbox, not the page, so a badge does not fall when somebody scrolls), `POST .../read` and `POST .../:id/read`, `GET /me/notification-settings` (**every kind**, with the value in force and whether it is the member's own -- a client given only the departures would need the defaults too, which is how a second copy of them gets written), `PUT /me/notification-settings/:kind`, and `PUT`/`DELETE /me/quiet-hours`. **The deep link is a pair, not a URL**: a URL built in the API would put the web's routing table in the API, and Phase 4's second client would have to accept it or ignore the field. The pair alone was not enough -- a profile is reached at `/u/{username}` and a group at `/groups/{slug}` while `subject_id` is the canonical UUID (rule 1) -- so the store resolves a `subject_label` at read time, left null when the subject no longer exists. `NOTIFICATION_TEXT`, `notificationLine()` and `notificationPath()` in `@fmip/contracts` are the sentences and the routing table (moved there on 2026-09-20 so e-mail and push share them; `apps/web/src/lib/notification-links.ts` re-exports), keyed by the contract's union so a kind added without words does not compile; `named` is a field rather than something inferred from capitalisation, because inference would work until somebody rephrased one. `components/notification-list.tsx` renders an unopenable notification **without** a link rather than hiding it -- what happened is still true, only the destination is gone -- and says out loud when one was held. T-273 applies both rules at emission. **Quiet hours delay and never discard**: `quiet_hours_end` gives the instant the window closes on the member's own clock, `deliver_after` is set to it and `held_reason` says so, and the notification surfaces when they are awake -- dropping a moderation decision because it landed at two in the morning would be the product deciding a member did not need to know. **The frequency cap drops**: over `NOTIFICATION_HOURLY_CAP` nothing new is written and the newest one of that kind carries `held_count`, incremented in SQL so two suppressions racing do not lose one. A row per suppressed event would be the flood again with a note attached. `held_count` is a column rather than a number derived at read time because the things being counted were never written -- the first attempt inferred it from the hour's rows and could only ever say "1 more". Only two kinds are capped, and the rest happen at human speed. `..._community-analysis.sql` is T-260: `community_analysis` (one per fixture per author -- a match carries many opinions, which is the feature), `community_analysis_draft` (the only mutable thing in the epic: a draft is where somebody is still thinking, and a history of half-finished thoughts would be a surveillance feature), `community_analysis_submission` (an immutable **copy** rather than a reference, so a reviewer can say what they were looking at), `community_analysis_review` (one per submission by the primary key -- two reviewers disagreeing about one submission is a situation the product has no answer for, so it is made impossible rather than resolved arbitrarily; a reason is required even for an approval) and `community_analysis_version` (immutable, descended from the approval that allowed it, so "who approved this" is a foreign key rather than a note). **Its own tables, sharing nothing with `founder_analysis`** (rule 6): the one-line wrong version of this epic is a second `author_id` on the founder's table, and it would pass every test about content while making the founder's signature meaningless. `PL014` gates writing one at all on a live contributor grant, and `PL002` walls off submission and publication at kick-off -- but not the draft, because nobody has been shown it and nothing is being claimed. `community_analysis_state()` derives draft / submitted / approved / changes_requested / rejected / published from the rows, and published wins: a later refused revision does not unpublish what the public has already read. `apps/web/src/components/notification-settings.tsx` offers every kind with the value in force and whether it is the member's own, names the ceiling where there is one, and says in words that nothing is thrown away. T-432 adds the way out: `carry(kind, compose)` takes every due notification of a kind (past `deliver_after`, no claim yet), **claims it in `notification_delivery` before the send** (`..._briefing-notification.sql`: one row per notification, the outcome on each channel written once and refused a second time by `refuse_second_outcome()`), composes through the caller's function because this module knows nothing about what a notification describes, and carries it through the delivery port, recording `absent` when no channel exists -- a stated nothing, not a gap. The same migration widens the kind lists and the subject list for `briefing`. T-330's composition (2026-09-20): `carry()` takes every due notification of every kind (past `deliver_after`, created within a day, unclaimed) and sends the contract's sentence with the absolute link by e-mail and with the path as a push, a registered `Composer` per kind overriding that (briefings), on a five-minute timer; with no channel at all nothing is claimed. `WEB_ORIGIN` is the module's own reading of `WEB_BASE_URL`. `GET /me/push`, `POST`/`DELETE /me/push-subscriptions` (D-074) register a browser's push subscription for the member. |
| `src/modules/panel/` | The public match discussion boundary (blueprint 10.2, T-251) -- its own module rather than a corner of the fixtures one, because a fixture is a football entity and a panel is a place people talk, with a gate, a ceiling and a moderation surface. `panel.service.ts` is the public surface (`panel`, `permissionFor`, `post`, `removeOwn`) and **decides nothing about who may write**: the three triggers on `panel_post` do that, and `permissionFor` asks the same questions only to *explain*, so that when the two ever disagree the database wins and the member is told the truth a moment late. `internal/panel-store.ts` is every statement; it writes the post and lets the triggers refuse it rather than checking first. `panel.controller.ts` is `GET /fixtures/:id/panel` (**no session asked for, so a guest reads**), `GET .../panel/permission` (deliberately a second request, so the panel is the same bytes for everybody instead of carrying a viewer-specific `can_post`), `POST .../panel` and `DELETE .../panel/:postId` for the author's own removal -- a moderator's removal is a moderation decision and stays in the queue, where it has an actor and a reason beside it. A `PL014` refusal is handed back to `permissionFor` before it is worded, because from inside `member_may_contribute` a paused grant, a withdrawn one and one that never existed are the same fact, and to the member they are not: two have a moderator and a reason behind them. 429 for the ceiling and 403 for a judgement -- "not now" and "not you" are different answers. Paging is a cursor over `(created_at, id)` built from `created_at::text`, never from the `Date`: a cursor rounded to milliseconds sorts before the row it points at, and every page would repeat its last post. `panel.http.spec.ts` reads the panel with no cookie at all and checks every refusal for its words rather than its status code. The page half is `apps/web/src/components/match-panel.tsx`, rendered on the match page **outside** the `me !== null` branch that `MatchThreads` sits in -- copying that guard is how the public discussion would have become private with nobody deciding to, and `match-panel.spec.ts` asserts the ordering so that moving it has to be argued for. Every refusal has words where the compose box would be, keyed by the contract's union so a new one cannot be added without them; falling short renders the shortfall list and qualifying-and-waiting renders a different sentence, because "reach a rating of 70" said to somebody with 82 is worse than silence. A removed post stays as a tombstone naming which kind of removal it was. `lib/panel-actions.ts` checks no permission at all and shows the sentence the API sent: a third copy of the rule would be the one that goes stale first. T-252 adds `panel-social.service.ts` and `internal/panel-social-store.ts` to the same boundary -- a reaction hangs on a panel post, and following a contributor is what a reader of a panel does next -- and **neither file contains `member_may_contribute`**, which is the acceptance criterion rather than an omission: reacting and following are open to any member, and a gate on either would be a second, quieter approval nobody decided to create. `panel-social.controller.ts` is `PUT`/`DELETE /panel-posts/:id/reactions/:reaction`, `GET /me/followed-members`, and `GET`/`PUT`/`DELETE /members/:username/follow`; `PUT` and `DELETE` rather than `POST` because both are idempotent, so a retried request after a dropped connection changes nothing. A block refuses with **one sentence in both directions**: which of the two blocked the other is not something either should learn here. The **counts** travel on the public panel and carry no `mine` -- on a document fetched without a session it could only ever be false, which reads as "you have not reacted" when the truth is "nobody asked" -- so the viewer's own reactions ride on `PanelPermission` instead, the request that already depends on who is asking. One query for a whole page of tallies, not one per post. The page half is `apps/web/src/components/panel-social.tsx`: the reaction row and the follow button appear for **any signed-in member**, asked as `signedIn` and never as `may_post` -- a surface that showed them only to approved contributors would be a second gate nobody decided to create, and it would look perfectly reasonable in review, so `panel-social.spec.ts` sweeps the code (comments stripped, the way T-321 does it) for the words that would mean one. The six come from `PANEL_REACTIONS` rather than a second list on the page, the pressed state is in the `aria-label` and not only in a border, and a guest sees the counts without buttons. `lib/panel-social-actions.ts` checks nothing and sends `PUT`/`DELETE`, so a double tap changes nothing; whom the viewer follows is fetched **once for the page** (`fetchFollowedMembers`) rather than a status call per author. |
| `src/modules/social/` | The social graph boundary (blueprint 8.1): friends, requests, blocks. It imports the moderation module to explain a PL004 refusal, never to make one. `social.service.ts` is the public surface (`friends`, `requests`, `blocked`, `status`, `request`, `accept`, `withdraw`, `unfriend`, `block`, `unblock`, `areFriends`); `internal/social-store.ts` is every statement, writing the pair as `LEAST/GREATEST` and letting the T-200 triggers do the refusing; `social.controller.ts` is `/me/friends`, `/me/friend-requests`, `/me/blocks` and `/me/friend-status/:username`, all under `/me` because none of these answers is public. Reaching a member needs a verified e-mail; getting away from one never does. `social.schema.spec.ts` (T-200) covers what the database guarantees — the ordered pair, `PL003` across a block, the block ending the friendship, a lifted block not restoring it; `social.http.spec.ts` (T-201) covers the surface, including a friends-only profile visible to a friend. |
| `src/modules/ingestion/` | The ingestion boundary. `EntityResolverService` (public, in `ingestion.service.ts`) resolves a provider id to an internal UUID or queues it; `internal/resolver.ts` is the pure logic over a `MappingStore` port; `internal/postgres-mapping-store.ts` is the SQL. T-071: `IngestRunsService` (`start`, `finish`, `track`) records every job run in `ingest_run` and logs a failed or partial run as an `ingest.failed` event; `internal/run-store.ts` is the SQL; `ingestion.controller.ts` is `GET /health/ingestion` (newest runs, last failure, failures in the last day, running). `ingest-runs.http.spec.ts` proves a failure is visible without SSH. T-026 (D-049): `internal/sources.ts` decides which provider answers which job from the environment (`replay` by default; the `live` profile splits football-data.org and Highlightly by module and never blends them) and holds `BudgetedTransport`, which answers 429 once Highlightly's daily ceiling is spent; `internal/ingest-store.ts` is every writer, each an upsert with `WHERE ... IS DISTINCT FROM` so an unchanged row is not written and the stream is not woken; `ingestion-jobs.service.ts` is the five jobs (`fixtures`, `live`, `lineups`, `standings`, `post_match`), each inside `IngestRunsService.track`, a provider refusal recorded as `partial` rather than thrown; `ingestion-scheduler.service.ts` is the BullMQ queue and worker, off unless `INGESTION_SCHEDULE=on`. T-027: `coverage.service.ts` (public) recomputes a season's seven coverage rows from the rows that exist — `internal/coverage-rules.ts` is the pure rule (all present `available`, some `limited`, none `not_supplied`, an admin's `delayed` kept only while nothing has arrived), `internal/coverage-store.ts` is what "should have this by now" means per module plus `freshness`, which reads the newest `updated_at` of the data itself rather than a job's clock. The jobs that can change an answer recompute at the end and count the writes. `coverage.spec.ts` argues with the rules. `sources.spec.ts` covers the profiles and the budget; `ingestion-jobs.spec.ts` runs all five against a real database on the replay source and proves the second run writes nothing. |
| `src/modules/identity/` | The identity boundary: `/auth/*`. `identity.service.ts` is public (`IdentityService`: register, login, authenticate, logout, verify e-mail, password reset, `userById`, `userByUsername`, `hasRole`; cookie helpers). `identity.controller.ts` validates bodies into the `@fmip/contracts` shapes. `internal/`: `password.ts` (scrypt), `tokens.ts` (random tokens, HMAC), `cookies.ts` (the session cookie, by hand), `validation.ts`, `identity-store.ts` (SQL), `mailer.ts` (the `MAILER` port; `LogMailer` prints). `identity.spec.ts` is unit; `identity.http.spec.ts` is the security suite against the real schema. Since D-073 `MAILER` is `internal/delivery-mailer.ts`: identity's mail leaves by the delivery port's e-mail channel and is printed where there is none. |
| `src/modules/forecast/internal/model-client.ts` | The API's side of the internal contract with the model service (T-063): `ModelClient.forecast()` and `.health()`, plus `contractProblems()`, which refuses any response that drifted from `@fmip/contracts`. Failure is a value. |
| `src/modules/forecast/` | The forecast boundary (T-064). `forecast.service.ts` is public: `ForecastService.compute(fixtureId, kind)` asks the model and stores the answer as the next immutable version — an `available` answer with probabilities re-rounded to total exactly 1, or an `unavailable` one with its reason (`competition_not_mapped`, `model_unreachable`, `contract_violation`, or the model's own) — and `.versions(fixtureId)` serves them oldest first with `coverage` and `last_updated_at` from the latest (D-030). `forecast.controller.ts`: `GET /fixtures/:id/forecasts` (public) and `POST` (admin session; the jobs of E2 call the service directly). `internal/forecast-store.ts` is the SQL, one transaction per version; `internal/rounding.ts` is `roundToTotalOne`. `forecast.spec.ts` is unit; `forecast.http.spec.ts` proves numbering, immutability and the probability CHECK against the real schema, and stores a live answer when `MODEL_SERVICE_URL` is set. Evaluation (T-066): `evaluation.service.ts` (`EvaluationService.evaluateFixture` scores every available version of a finished fixture against its full-time score, once; `.performance(competitionId, seasonId)` aggregates pre-kick-off evaluations per model version and kind, with the gaps counted — D-031), `evaluation.controller.ts` (`GET`/`POST /fixtures/:id/evaluations`, POST admin only; `GET /competitions/:id/model-performance?season=`), `internal/scoring.ts` (log loss, Brier, correct, scoreline hit — the same definitions as the Python `metrics.py`), `internal/evaluation-store.ts`; `scoring.spec.ts` and `evaluation.http.spec.ts`.  T-110/T-111: the Power Index (blueprint 6.1) — `internal/power-index.ts` is the published weights and the combination rule (a missing component is `not_supplied` and its weight is redistributed; `completeness` is published beside the number), `internal/power-index-measure.ts` is the pure measurement of strength, opponent-adjusted form, venue record and rest as percentiles within the division, `internal/power-index-store.ts` is the SQL over the training store and our own schedule, and `power-index.service.ts` computes, stores an immutable row and reads back the newest. `power-index.spec.ts`, `power-index-measure.spec.ts` and `power-index.store.spec.ts` cover the arithmetic, the measurement rules and the schema; `power-index.service.spec.ts` measures Manchester United and Liverpool against the real 2024/25 Premier League.  T-120: `internal/forecast-triggers.ts` is the pure rule for which version is due (once each, never after kick-off, a confirmed line-up ahead of an unwritten early one, and never `lineups_predicted` because nothing supplies predicted line-ups); `forecast-triggers.service.ts` runs it across the window and computes the Power Index on the same pass. The scheduler gives it its own `forecast-versions` tick, because producing a forecast is not ingestion and must not appear in `ingest_run`. |
| `src/modules/forecast/model-client.spec.ts` | The contract test: validates the golden examples the model service writes, the client's behaviour on drift, HTTP errors and an unreachable service, and, when `MODEL_SERVICE_URL` is set (CI starts the service), the live service. |
| `src/modules/fixtures/` | The fixtures boundary's read side. T-030: `GET /scores` — `fixtures.service.ts` is public (`FixturesService.scores(filters, viewerId)` plus `parseScoresQuery`); `scores.controller.ts` reads the session if there is one; `internal/scores-query.ts` parses dates, the IANA zone, flags and ids and names every bad field at once; `internal/scores-store.ts` is the SQL, with the date range turned into instants by Postgres in the user's zone (`AT TIME ZONE`), scores and list incidents aggregated per fixture, and `last_updated_at` the newest change across fixture, scores and incidents; `internal/arrange.ts` pins favourites (ranks from the profile boundary's `favouriteRank`) and groups the rest by competition under its country. `internal/freshness.ts` (T-083, D-045) is the rule that marks a live fixture unchanged for `STALE_LIVE_AFTER_MS` as `stale`; every card and match header carries `freshness` (`freshness.spec.ts`). `scores.spec.ts` is unit (parsing, grouping); `scores.http.spec.ts` proves the timezone boundary, filters and pinning against the real schema. T-033: `GET /fixtures/:id` — `match-centre.controller.ts`; `FixturesService.matchCentre()` assembles header, timeline, statistics, line-ups, form and head-to-head, each a `Covered` module; `internal/match-centre-store.ts` is the SQL (eight tables, `last_updated_at` as the newest change across them); `internal/covered.ts` is the rule that turns rows plus the season's declared coverage into one state (`covered.spec.ts`). `match-centre.http.spec.ts` builds a five-fixture cluster and checks every module. T-032: `stream.controller.ts` — `GET /scores/stream` and `GET /fixtures/:id/stream` (and `GET /health/live`, the subscriber count, T-071), server-sent events: a full snapshot first, a new one on every (debounced) change, a `heartbeat` every 15 s, `stale` when the feed breaks; `internal/change-feed.ts` is the `LISTEN fixture_change` connection fanned out in-process; `internal/sse.ts` the framing and the debounce (`sse.spec.ts`); `stream.http.spec.ts` reads events off a real port while a score changes in the database. `stream.controller.ts` also carries T-254: a change to `panel_post` is a `panel` event with no payload beyond the time -- the page re-reads the discussion -- and never a snapshot, because the panel is not in the match centre (D-068). |
| `src/modules/consensus/` | The community consensus boundary (T-134, blueprint 6.6): `GET /fixtures/:id/consensus`, public. `consensus.service.ts` is the whole public surface; `internal/distribution.ts` is the arithmetic (pure — `crowd`, `weighted`, largest-remainder shares that sum to exactly 1) and `internal/consensus-store.ts` the one aggregate over `prediction_version` and `rating_snapshot`. Only established raters weight the second distribution and nothing is published below `MIN_CONSENSUS_SAMPLE` (D-052); where the weighted distribution cannot be built it is `null` and the module is `limited`, never the crowd distribution under the other label. `consensus.spec.ts` is the arithmetic, `consensus.http.spec.ts` the parts only a database decides. T-136 adds `consensus-list.controller.ts` — `GET /consensus?fixtures=a,b,c`, one query for a page's worth of matches, capped at `MAX_CONSENSUS_FIXTURES`, rejecting a malformed id rather than dropping it. |
| `src/modules/predictions/` | The predictions boundary (T-050): `predictions.service.ts` is public (`PredictionsService.submit(who, fixtureId, body)` — refuses an unverified e-mail, an unknown fixture and a match that has kicked off, validates, writes the next immutable version; `.own()`); `predictions.controller.ts`: `GET`/`PUT /fixtures/:id/prediction`, 401 for guests, 403 `email_unverified`, 409 `locked`, 400 with every bad field; `internal/validation.ts` (the rules the database also enforces, pure, `predictions.spec.ts`); `internal/prediction-store.ts` (the SQL: upsert `user_prediction`, insert `prediction_version` as MAX + 1 under the row lock). `predictions.http.spec.ts` proves guests blocked, verification required, versions retained and immutable, lock after kick-off; `lock.http.spec.ts` (T-051) proves the database has the last word with the API clock skewed both ways and on a direct write. T-052: `settlement.service.ts` (`SettlementService.settleFixture` writes one run and its rows in a transaction; `settleDue` is the job's pass), `settlement.controller.ts` (`GET /fixtures/:id/settlements` public aggregate, `POST /fixtures/:id/settle` and `POST /settlements/run` admin), `internal/settle.ts` (the rulebook: settle, void with reason, or wait — pure, `settle.spec.ts`), `internal/settlement-store.ts`; `settlement.http.spec.ts` proves idempotent re-runs and void → settled supersession. `SettlementService.settledHistory / predictors / recentlySettledUsers` are what the reputation boundary reads. T-056: `history.controller.ts` — `GET /users/:username/predictions`, `GET /me/predictions` (fixture, every version, the one that stands, current settlement; newest kick-off first; `internal/history-query.ts` is the paging, `history.spec.ts`); visibility asked of the profile boundary (`ProfileService.predictionHistoryAccess`) before anything is serialised; `PostgresPredictionStore.history` is the SQL. `history.http.spec.ts` proves version, time, settlement and the visibility switch. T-246: `GET /groups/:slug/fixtures/:fixtureId/predictions` on `history.controller.ts`, `PredictionsService.callsOn(fixtureId, userIds)` and `PostgresPredictionStore.callsOn` -- three queries whatever the group's size, and **the settlement is read, never recomputed** (D-063): `toSettlement` is one mapper for the single read and the bulk one. Visibility is `ProfileService.predictionHistoryAccess`, asked once per member who actually called the fixture; the silent and the withheld are counted separately so the comparison never reports a smaller group than exists. The module imports `GroupsModule` for `audience()`, the same direction as T-243. `group-comparison.http.spec.ts`. |
| `src/modules/reputation/` | The reputation boundary. T-053: `reputation.service.ts` is public (`ReputationService.current`, `.recompute(userId)` — snapshot only when the inputs changed —, `.recomputeForFixture`, `.recomputeDue`; re-exports the formula); `reputation.controller.ts`: `GET /me/rating`, `GET /users/:username/rating`, `POST /me/rating/recompute`, `POST /ratings/recompute` (admin); `internal/formula.ts` is the versioned formula config and the pure computation (`formula.spec.ts`); `internal/rating-store.ts` the snapshots. `reputation.http.spec.ts` proves recomputability and the favourite-picker penalty end to end. T-054: `career-points.service.ts` (`CareerPointsService.award` writes the missing ledger rows, `.summary`), `internal/points.ts` (the rules and the pure `awardsFor`), `internal/eligibility.ts` (blueprint 9.4 eligibility without a points input), `internal/points-store.ts`; endpoints `GET /me/points`, `GET /users/:username/points`, `POST /me/points/award`, `GET /me/eligibility` in the same controller; `points.spec.ts`, `career-points.http.spec.ts`. T-055: `GET /leaderboard` in the same controller; `internal/leaderboard.ts` is the rules (`leaderboard@1.0.0`: floor, presets, page limits, D-037) and `parseLeaderboardQuery`; `PostgresRatingStore.board` is the SQL (newest snapshot per member, `rank()` before paging, active accounts only); `ReputationService.leaderboard` derives the tier; `leaderboard.spec.ts`, `leaderboard.http.spec.ts`. T-243: `GET /groups/:slug/leaderboard` in the same controller, `ReputationService.leaderboard(query, among)` and `PostgresRatingStore.board(..., among)` -- **one optional argument, and nothing else changes**, because a second method is where a second formula begins (D-060); the module imports `GroupsModule` for `audience()` and never learns what a group is; `group-leaderboard.http.spec.ts` compares the two boards' *rules* rather than their numbers. T-250 (schema half): `contributor.schema.spec.ts` covers what the database guarantees about who may post on a public panel -- the `contributor_eligibility_input` view refuses an INSERT and carries no verdict column, a member with no rating keeps a row with a null rating rather than being dropped, `member_under_sanction` sees a scope it was never told about, a grant and every event on it are immutable, `PL013` for the transitions that cannot happen, and the two assertions the task exists for: a granted member who qualifies for nothing may post, and a member who meets every measurable requirement may not. The contract is `packages/contracts/src/contributor.ts`, where computed eligibility and a granted privilege are separate types with no field in common. T-250 (service half): `contributor.service.ts` is the public surface (`eligibilityOf`, `statusOf`, `candidates`, `grant`, `change`) and has two verbs that are not the same verb -- there is no method that reads eligibility and writes a grant; `internal/contributor-store.ts` is every statement, applying the conduct window **in SQL** so that one clock decides while the number still comes from `ELIGIBILITY_V1`, and writing each grant or event with its `audit_log` row in one transaction, `previous` carrying the standing it had before; `internal/eligibility.ts` is the four requirements as a pure function at `privilege-eligibility@1.1.0` (the two thresholds confirmed, conduct added by D-059) and names every shortfall at once rather than the first. `contributor.controller.ts` is `GET /me/contributor` for the member and `/admin/contributors` for the approver (`moderator` or `admin`): list, one member, `POST` to grant, and `pause` / `resume` / `withdraw`, each needing a reason because the member is told it, and the server -- not the caller -- names the contributor-rules version being accepted. `GET /me/eligibility` keeps its published shape and is now a projection of the same computation rather than a second one. `contributor.spec.ts` covers the requirements, `contributor.http.spec.ts` the gate, the lifecycle and the audit rows. |
| `src/modules/<boundary>/` | The remaining boundaries from `02-architecture.md`. Empty until built. |
| `Dockerfile` | Multi-stage build. Built from the repository root, not from `apps/api`. Compiles through Turbo so `@fmip/contracts` is built first, and deploys with `pnpm deploy --legacy`, because pnpm 10 refuses to deploy a workspace that does not inject its packages. |
| `vitest.config.mts` | Vitest transformed by SWC rather than esbuild (D-019). |

**Cleaning up after a database spec.** `src/testing/cleanup.ts` is the helper
(`withTriggersOff(pool, fn)`) and `cleanup.spec.ts` is the rule made
enforceable: it reads every spec in the tree and fails on a new offender,
holding the remaining unconverted files in a list that may only get shorter.
The rule had been prose since T-120 and was broken in thirteen files by the
time anything checked -- two of which a hand search missed.

**What it was actually costing.** `ALTER TABLE` takes an `ACCESS EXCLUSIVE` lock
on every table it names. Under a parallel run that is contention in the
`afterAll`, and the cleanups were one all-or-nothing transaction, so a blocked
one rolled back and left **everything** -- accounts with settled ratings
included. Those then sat in the database and put extra members on the global
leaderboard, so the next run failed in a different spec, for a reason with no
visible connection to the first. Two generations of that debris were found on
2026-09-16, which is how long it had been happening. The converted cleanups take
no lock at all.

Rows a cascade cannot reach -- the
immutable ones, guarded by `refuse_change()` -- are deleted on one dedicated
connection with `SET session_replication_role = 'replica'`, which is scoped to
that session. Never `ALTER TABLE ... DISABLE TRIGGER`: that is global, and while
it is off a suite running in parallel that asserts the same table is immutable
passes without testing anything. Put the setting back **before** deleting the
`user_account` rows, because `replica` turns off foreign-key triggers too and a
cascade does not run while it is set -- the credentials, sessions and tokens
would be left behind. The moderation specs are the worked example.

Delete every table that references the accounts, not only the ones the spec
wrote itself. `rating_snapshot` and `points_transaction` are ON DELETE
RESTRICT, and they are written by whoever recomputes a recently settled member
-- `POST /ratings/recompute` covers every such member, not only its own
fixtures -- so a predictions spec's accounts can own rows no test in that file
created. Miss one and the account delete fails, the account survives the run,
and it fails the same way for good. Let the cleanup run whether or not the
tests passed, and do not wrap it in one transaction: a `ROLLBACK` on the last
statement puts back everything the earlier ones removed.
`predictions/history.http.spec.ts` is the worked example.

**`/health` is liveness, not readiness.** It reports that the process is serving
HTTP and nothing else. It deliberately says nothing about Postgres or Redis: no
client for either exists yet, and claiming a dependency check that never runs is
the failure rule 3 exists to prevent. Readiness arrives with T-008.

One directory per module from `02-architecture.md`. Each will contain:

```
<module>/
  <module>.module.ts        # NestJS wiring
  <module>.controller.ts    # HTTP surface
  <module>.service.ts       # PUBLIC — other modules may import this
  internal/                 # PRIVATE — never imported across modules
  dto/                      # request/response shapes
  <module>.spec.ts          # unit tests
```

### `apps/model`

Python 3.12+, managed with a plain venv. `pnpm --filter @fmip/model setup` creates
`.venv` and installs the package with its dev tools; the Turbo scripts run that
interpreter through `scripts/py.mjs`, or `python` from PATH when there is no
venv (CI). On the maintainer's host, pip needs the proxy:
`python -m pip install --proxy http://127.0.0.1:3128 ...`.

| Path | Purpose |
|---|---|
| `pyproject.toml` | Package `fmip-model`: runtime deps `httpx`, `psycopg`; dev `pytest`, `ruff`, `mypy` (strict). Ruff and mypy configuration. |
| `package.json`, `scripts/py.mjs` | Turbo hooks: `lint` (ruff check + format), `typecheck` (mypy), `test` (pytest), `build` (import check). |
| `fmip_model/training/sources.py` | The two sources (D-016) with their terms URL and note, and the URL builders. Every load records these. |
| `fmip_model/training/football_data.py` | Parses a football-data.co.uk CSV into match rows: results, half-time, shots, and closing 1X2 odds from the first bookmaker set present (`B365`, then `Avg`, `PS`, `WH`), naming the source. Unsupplied is `None`, never a guess. |
| `fmip_model/training/clubelo.py` | Parses Club Elo CSV (`Rank,Club,Country,Level,Elo,From,To`). |
| `fmip_model/training/store.py` | Writes to the `training` schema: opens a `source_load` row, upserts on natural keys, closes the load as succeeded (hash, row count) or failed (error). |
| `fmip_model/training/load.py` | The CLI: `python -m fmip_model.training.load football-data --seasons 2425 --divisions E0 SP1` and `clubelo --days 2025-08-01`. One load row per (source, scope); a failure writes no rows and is recorded. |
| `fmip_model/model/poisson.py` | Expected goals → scoreline matrix (0–10 each side, Dixon-Coles low-score correction, renormalised) → `Outcome`: home/draw/away, expected goals, most likely scorelines, and `rounded()` so the three displayed probabilities total exactly 1. |
| `fmip_model/model/dixon_coles.py` | The fit (T-061): time-weighted penalised maximum likelihood over attack, defence, home advantage and `rho`, with a ridge and an Elo prior on net strength. `FittedModel.predict(home, away)` reads everything off the matrix; an unknown team is an error, not a guess. |
| `fmip_model/model/data.py` | Reads matches and Elo from the `training` schema into model inputs, with the fit date as a hard boundary against leaks. |
| `fmip_model/model/version.py` | `ModelVersion`: name, version and the frozen constants (`xi`, ridge, Elo weight, scale, max goals). `BASELINE` is `dixon-coles-elo@0.1.0`. A changed constant is a new version. |
| `fmip_model/backtest/` | T-062. `metrics.py`: log loss, Brier, reliability bins, calibration error. `market.py`: de-margined implied probabilities from closing odds. `walk_forward.py`: fit on the past, forecast the day, refit weekly, never see the future; scores the model, the market and uniform on the same matches. `report.py`: Markdown + JSON per model version and scope, with the D-016 verdict. `__main__.py`: `python -m fmip_model.backtest --divisions E0 --from … --to … --history-from …`. |
| `reports/<model-version>/` | Committed backtest reports: the evidence behind a model version. Never edited by hand. |
| `fmip_model/service/` | T-063, the FastAPI service. `contract.py`: the Pydantic twin of `packages/contracts/src/forecast.ts`. `forecaster.py`: aliases catalog team ids to training names, fits once per (division, day) and caches, answers `available` with probabilities that total 100%, expected goals, most likely scorelines, leading factors and what data was used, or `unavailable` with a reason (`team_not_mapped`, `no_history`, `division_not_loaded`). `store_source.py`: the training store as its source. `app.py` / `__main__.py`: `GET /health`, `POST /forecast`; `python -m fmip_model.service` on `MODEL_PORT` (8000). |
| `contract/*.example.json` | Golden request and response examples, written by the service's own tests and validated by `apps/api`'s contract test. Regenerated by `pytest`, never edited by hand. |
| `Dockerfile` | `python:3.12-slim`, dependencies from `pyproject.toml`, non-root, its own HEALTHCHECK. Built from the repository root. The compose `model` service publishes no port. |
| `tests/` | Parser tests on a real football-data file head (`fixtures/`); store and real-season fit tests against the database when `DATABASE_URL` is set; model tests on simulated seasons with known strengths. |

**The training store is a schema, not a package.** `training.source_load`,
`training.match` and `training.elo` live in the same Postgres as everything
else (D-028), and nothing in `public` references them. `apps/api` never
queries the schema; the model service is its only reader. Team names are
text there, by design: the sources identify teams by name and the data is
offline research (D-014), so mapping historical names onto the catalog is the
model's job at training time, not a reason to bend rule 1.

### `apps/web`

| Path | Purpose |
|---|---|
| `src/app/[locale]/layout.tsx` | Root layout. Owns `<html lang dir>`; 404s an unshipped locale; sets `metadataBase` and the default canonical, alternates and robots rule through `pageMetadata` (T-039); renders the skip link and the `#content` target the keyboard lands on (T-081). |
| `vitest.config.mts` | The `@/` alias the app compiles against, and JSX compiled with the automatic runtime (the app's `tsconfig.json` says `preserve`, which is Next's to honour, not a test's) so a spec can render a component. |
| `src/app/manifest.ts` | `/manifest.webmanifest` (T-082, D-042): name, start page, `standalone`, colours, the 192/512 icons and a maskable one. |
| `public/sw.js`, `src/components/service-worker.tsx` | The service worker and its registration (T-082): caches the shell only (offline page, manifest, icons, immutable build assets), network first for pages, the offline page without a network; never caches live data or `/api/`. |
| `public/icons/`, `scripts/make-icons.mjs` | The PWA icons and the script that draws them (no image library). |
| `src/app/[locale]/offline/page.tsx` | The offline shell: says the app is offline and that nothing cached is shown as current. |
| `src/app/robots.ts`, `src/app/sitemap.ts` | `/robots.txt` (pseudo-locale, the web app's API routes and member pages disallowed; the sitemap named) and `/sitemap.xml` (static pages plus every active competition and team per indexable locale, from the catalog per request) — T-039, D-040. On a deployment holding demonstration data both refuse: `Disallow: /` with no sitemap, and an empty map (T-087). |
| `src/lib/demonstration.ts`, `src/components/demonstration-banner.tsx` | Whether this deployment's football is development fixture data (`DEMONSTRATION_DATA`), and the undismissable band that says so on every page — T-087, D-065. The banner calls `connection()` **before** reading the environment, because the variable arrives at runtime and a check made during prerendering would bake "not demonstration data" into a page of invented scores. `demonstration.spec.ts` covers the banner, the metadata, `robots.txt`, the sitemap and the boot guard. |
| `src/lib/announce.ts` | Live-score announcements in words (T-081, D-041): `describeChange`, `scoresAnnouncements`, `matchAnnouncements` — goal, kick-off, full time, correction, red card — for the polite live regions of the scores page and the match centre. `announce.spec.ts` covers the wording. |
| `src/lib/seo.ts` | The SEO surface (T-039): `siteUrl` (`SITE_URL`), `canonicalUrl`, `pageMetadata` (canonical, language alternates with `x-default`, robots, Open Graph) and the schema.org JSON-LD builders (`WebSite`, `SportsEvent`, `SportsOrganization`, `SportsTeam`, `Person`, breadcrumbs). Demonstration data forces `noindex` and marks the Open Graph title; the `<title>` is marked by `title.template` on the locale layout instead, because nine pages set their metadata without calling this (T-087). `seo.spec.ts` covers them. |
| `src/components/json-ld.tsx` | Renders structured data as `<script type="application/ld+json">` on the server, `<` escaped. |
| `src/app/[locale]/page.tsx` | Placeholder home page. The scores page replaces it in T-031. |
| `src/app/[locale]/register`, `login`, `forgot-password`, `reset-password`, `verify-email` | The account pages (T-040/T-041). Forms are `ActionForm` over a server action; `verify-email` spends the token on render. |
| `src/app/[locale]/following/page.tsx` | The Following feed (blueprint 12.1, T-333): `GET /me/feed` rendered in the API's order, the first line saying the window, what is followed and the ranking rule by name, every item with its kind, its moment, a link to the thing by id, its `because` chips and its rank. Adds nothing to the ranking and hides nothing of it. Helpers in `src/lib/feed.ts` (total records over the contract's kinds, signals and reasons). |
| `src/app/[locale]/news/page.tsx` | The news page (blueprint 3.1 and 3.2, T-143): four sections over `GET /news?section=`, the filters as a GET form from the catalogue lists, every reason the API gives as a catalogue sentence where the list would be, freshness from when the feeds were last read and a stale notice past three hours (rule 4). A card is the publisher's words and a link back (D-061), `lang`-tagged with the version's language, with entity chips by id. Draws the editor's select/clear `ActionForm`s only for a viewer the editor's list (`fetchDebates`) answered; the actions are `src/lib/debate-actions.ts`. |
| `src/app/[locale]/news/story/[id]/page.tsx` | The story page (blueprint 3.3, T-144) under D-061: the original's words and the way to the publisher (`Read the original` where a free feed grants no body, the body itself only when `rights = 'full_text'`), the languages it exists in as links (`?language=`), corrections or the sentence that none were noted, entity chips and a link to the match centre for the three prediction products (rule 6: linked, never copied), the other publishers' reports, save and share named as not built, and the feed-read freshness. |
| `src/app/[locale]/leaderboard/page.tsx` | The leaderboard (blueprint 9.3, T-055): members ranked by current rating behind the minimum-sample filter (`?min=`, `?page=`), presets and floor from the API, a 400 for a filter under the floor said out loud, an unreachable API likewise. |
| `src/components/match-threads.tsx` | Opening a group's thread about a match, from the match (T-248). One button per group and the same button whether or not the thread exists, because opening is idempotent (T-244) -- so nothing is asked before the page draws itself. "You are in no group" and "your groups could not be fetched" are separate statements. |
| `src/components/group-comparison.tsx` | What a group called a match (T-246, T-248). **The verdict is the stored settlement**, never worked out here: a component that decided for itself whether somebody was right would be the second settlement D-063 refuses, on the surface. "Not settled yet" and "Void" are verdicts; a blank would be a member the product had nothing to say about. The silent and the withheld are counted separately. `group-comparison.spec.ts` guards both, and the match-page control. |
| `src/lib/thread-actions.ts` | Opening a thread over a server action, then redirecting into the room (T-248). |
| `src/lib/conversation-title.ts` | What to call a conversation in a list (T-244). A group's room and every one of its match threads carry the same `group` and no members, so a list titled by the group alone would show several entries with one name; **every kind gets a branch** and the guard reads `CONVERSATION_KINDS` from the contract. A thread is named by its match *and* its group; one whose fixture no longer resolves says a match is missing rather than naming one the product cannot see. `threadStanding` is the score the match has now (rule 4). `conversation-title.spec.ts`. |
| `src/lib/leaderboard.ts` | The leaderboard page's pure helpers: reading `?min=` and `?page=`, the API query, the links that keep state, rating / tier / status labels. `leaderboard.spec.ts` covers them. |
| `src/app/[locale]/player/[id]/page.tsx` | The player page (blueprint 5.3, T-037): identity, current team, career spells, the record per season and competition and the recent-match log with a season selector (`?season=`); statistics not held are named, not shown as zero; 404 for an unknown player, an unreachable API said out loud. |
| `src/lib/player.ts` | The player page's pure helpers: age, spell period, role and appearance labels, the season selector over record and log. `player.spec.ts` covers them. |
| `src/app/[locale]/team/[id]/page.tsx` | The team page (blueprint 5.2, T-036): header with ground and followers, competitions with the table context, next and previous match, fixtures and results from the team's side, squad by position; 404 for an unknown team, an unreachable API said out loud. |
| `src/lib/team.ts` | The team page's pure helpers: squad grouping by position, ordinals and the context line, a match read from the team's side. `team.spec.ts` covers them. |
| `src/app/[locale]/competition/[id]/page.tsx` | The competition page (blueprint 5.1, T-035): header, season strip (`?season=`), the table, results and fixtures linking to the match centre, top scorers, the coverage line; each module with its coverage from the API, 404 for an unknown competition or season, an unreachable API said out loud. |
| `src/lib/competition.ts` | The competition page's pure helpers: the season the URL selects, the links that keep it, fixture and form labels. `competition.spec.ts` covers them. |
| `src/app/[locale]/search/page.tsx` | Entity search (T-038): a plain GET form over `?q=`, results with kind, secondary line and the alias that matched, linking to competition, team and player pages; too short a term is a hint, an unreachable API is said out loud. |
| `src/lib/search.ts` | The search page's pure helpers: the term from the URL, the API query, where a result leads, the alias note. `search.spec.ts` covers them. |
| `src/app/[locale]/scores/page.tsx` | The scores page (blueprint 4.1, T-031): one day in the viewer's zone (`?tz=`, else the member's, else UTC), the yesterday / today / next-five-days strip, All / Live / Favourites-only filters, favourites pinned, the rest grouped by competition under its country. An unreachable API is said out loud, never shown as a quiet day. |
| `src/components/score-card.tsx` | One match on the list, linking to its match centre: status or clock, teams with red-card marks, score, competition / stage / round / leg / aggregate, kick-off and venue, goals / red cards / VAR, then the labels: the scores coverage state and, for what the platform does not have yet, "Forecast: not on this page yet", "Community: unsupported", "Watch: unsupported". |
| `src/components/live-scores.tsx` | Client component (T-032): renders the server's snapshot (competition headings link to the competition page since T-035; a polite live region says every change in words since T-081), subscribes to `/api/scores/stream`, replaces the whole picture on every `snapshot`, and shows the freshness line — connecting, live (with the last update time), stale after 45 s without a heartbeat, unavailable when no picture ever arrived. |
| `src/app/api/scores/stream/route.ts` | The browser's end of the stream (D-027): holds the matching connection to the API's `/scores/stream`, forwards the session cookie, passes the bytes through; 503 with a JSON error when the API is unreachable. |
| `src/lib/founder-actions.ts` | Publishing the founder's analysis (T-131) as a server action, so the session cookie never leaves the server and the page works without JavaScript. An empty optional section is sent as absent, never as an empty string. |
| `src/lib/forecast-diff.ts` | What changed between two forecast versions and what may be blamed for it (T-121): every input that moved with its before and after, and the sentence — one input named, several declared inseparable, or nothing changed. A confirmed-line-up version always carries the caveat that the model does not read line-ups. `forecast-diff.spec.ts` covers it. |
| `src/lib/power-index.ts` | The words the Power Index panel shows (T-114): a component's percentile as a sentence, the completeness sentence that names what was left out, the leading-factor phrase, and the bar width (`null` for an unmeasured component). `power-index.spec.ts` covers all of it. |
| `src/lib/live.ts` | Freshness of the stream (connecting / live / stale, T-032) and of the data: `isBehind` re-asks the T-083 rule on the client clock, `feedNotice` words the scores-page notice when the ingestion feed's latest run failed. `live.spec.ts` covers all of it. |
| `src/lib/news.ts` | The news page's pure helpers: reading the query (a malformed filter is dropped, not sent on to be refused), the API query, the page's own hrefs (`before` never carries over unless asked for), `SECTION_KEY` and `REASON_KEY` as total records over the contract's unions, entity chips' hrefs by id, `readStoryQuery` and `storyHref` for the story page, and `feedsStale`. |
| `src/lib/viewing.ts` | The Watch surfaces' pure helpers (T-314): reading a guest's `?territory=` (anything that is not a code is dropped, not sent on), the four states of the options module (`ask`, `not_supplied`, `nothing_listed`, `listed`) and five of the highlights module, `embeddable` (a player only from a source that grants one and only with one to show, T-315), and the links that keep a guest's pick with them. |
| `src/components/viewing-panel.tsx` | Where a match can be watched and its highlight afterwards (blueprint 11, T-314, T-315): one component in two shapes, the panel (match centre, with the territory chooser -- settings for a member, a plain GET form for a guest) and the line (lists). Four sentences, never a fifth; an unreachable viewing service is said, not rendered as an empty module. |
| `src/app/[locale]/watch/page.tsx` | The Watch page (T-314): the scores page's day in the same zone, the chooser, and one viewing line per match from one batch request; an empty day and an unreachable service each say so. |
| `src/components/viewing-desk.tsx`, `src/lib/viewing-actions.ts` | The editorial desk on the match page (T-313): for an editor, forms that declare coverage for the season in a territory, add a broadcaster, list the match on a service, set the official highlight page, and take either down with a reason -- server actions calling `/admin/viewing/*` and `/admin/fixtures/:id/*`, revalidating the match and Watch pages. Nothing here takes a player or a thumbnail. |
| `src/lib/scores.ts` | The page's pure helpers: reading the query (zone precedence, bad date → today), the API query and page links that keep state, the day strip, status / score / kick-off labels. `scores.spec.ts` covers them. |
| `src/app/[locale]/match/[id]/page.tsx` | The match centre page (blueprint 4.2, T-034): fetches `GET /fixtures/:id` (plus the forecasts, the Power Index and, when finished, the evaluations), renders it through `LiveMatch` with the Power Index and forecast panels, 404 for an unknown or malformed id, an unreachable API named out loud. Zone from `?tz=`, else the member's, else UTC. |
| `src/components/match-centre-view.tsx` | The layout of blueprint 4.2 from one `MatchCentre` payload: header (teams, score, status or clock, competition — linked to its page and season since T-035; team names link to team pages since T-036 and every line-up and incident name to the player page since T-037, stage, round, leg, aggregate, kick-off, venue, referee, attendance, last data update), timeline, statistics (an unsupplied side is "–"), line-ups with formation, coach and captain, recent form, head-to-head, the season's coverage per module, and the list of modules not built yet with why. Every module shows its coverage state beside its name. |
| `src/components/related-news.tsx` | Related news on the match centre (blueprint 4.2, T-145): the news page's cards for this match and its sides from `GET /fixtures/:id/news`, with the news page's freshness line; three states -- feeds never read, nothing linked, the list -- and never an empty box. |
| `src/components/match-summary.tsx`, `src/lib/summary-actions.ts` | The match summary on the match centre (T-413): a finished match's published version under the label that says a machine wrote it from the record, with the model, the version, the time and the parts it was written from; the sentence that says why there is none; the editor's regeneration form through a server action. |
| `src/components/briefing.tsx`, `src/lib/briefing-actions.ts` | The member's briefing on the Following page (T-431): the prose under its label with the window, the model and the time; the sentence that says why there is none; the button that asks, through a server action. |
| `src/app/[locale]/admin/campaigns/page.tsx`, `src/components/audiences-admin.tsx`, `src/components/campaigns-admin.tsx`, `src/lib/campaign-actions.ts` | Campaigns for administrators (T-332, D-075): audiences with their live sizes and a form whose fields are the closed vocabulary; campaigns with their send report (reached, held, muted, already told, failed) and a send form that asks why; every write is one audited API call. A member without the role is not told the page exists. Linked from the administration hub. |
| `src/components/live-match.tsx` | Client component: the server snapshot first, then `/api/fixtures/:id/stream`, whole payload replaced on every `snapshot`; the same freshness line as the scores page. Also listens for the stream's `panel` event (T-254, D-068) and asks the server to render the page again, collapsed to one refresh per burst, rendering nothing from the event itself; `live-match.spec.ts` reads the source for exactly that. |
| `src/app/api/fixtures/[id]/stream/route.ts` | The browser's end of one match centre's stream (D-027); 404 for a malformed id, 503 JSON when the API is unreachable. |
| `src/components/score.tsx` | A score that cannot be read backwards (T-153): `Score` renders the pair inside `dir="ltr"`, `LtrNumeric` wraps a score that is already a string, `ltrIsolate()` does the same with Unicode isolates inside a larger label. Every score on the product goes through one of the three, because `2 – 1` in a right-to-left paragraph otherwise renders as `1 – 2`. |
| `src/app/[locale]/predictions/page.tsx` | The Predictions page (T-137, blueprint 2.1): model forecasts, the founder's analysis, community consensus and the prediction leaderboard, as four sections rather than one ranked feed — the page rule 6 was written for. Fixtures come from the scores boundary; each product is fetched from its own list endpoint (T-136) and rendered by its own component. Every section states its own absence rather than disappearing. |
| `src/components/community-consensus.tsx` | The community forecast on the match centre (T-135, blueprint 4.2): both distributions, the honest absence of the weighted one, and the comparison with the model as a signed difference with both sides named — never an average. Takes a `Triple` of plain numbers rather than a forecast, so it cannot render one (rule 6, guarded by `three-products.spec.ts`). |
| `src/lib/triple.ts` | Three numbers for the three outcomes and the arithmetic on them (T-135): `sharesToPercentages` (totalling exactly 100), `difference` and `signed`. Deliberately typed to neither product, so the model panel and the community panel can share the rounding without sharing a type. `lib/forecast.ts`'s `percentages` delegates here. |
| `src/components/translated.tsx` | One message with the truth about where it came from (T-151): a string the locale has no translation for is rendered in English and wrapped in `lang="en"`, so the fallback is visible to a screen reader, to a browser and to anyone reading the markup. The span also carries `dir` for the source language, so on a right-to-left page the English is isolated and keeps its own punctuation order rather than lending it to the Arabic paragraph around it. `translated.spec.ts` renders both paths through `react-dom/server`. |
| `src/components/founder-analysis.tsx` | The founder's analysis where a reader meets it (T-132): `FounderAnalysisPanel` is the full entry for the match centre with every section and the earlier versions; `FounderAnalysisFeed` is the excerpt-and-link entry for the homepage, team and competition pages. Both always carry the author and the publication time, and neither can render a forecast — `three-products.spec.ts` beside it fails if that changes (T-133, rule 6). |
| `src/components/power-index-panel.tsx` | The Power Index on the match centre (T-114, blueprint 6.1): both sides' 0–100, and per team a breakdown with each component's weight, its standing said in words, the leading factors and the completeness sentence. A component nothing measured keeps its row and its reason and gets no bar at all — a zero-width bar reads as "measured, and bad". Wording in `src/lib/power-index.ts`, tested. |
| `src/components/forecast-panel.tsx` | The model forecast on the match centre (T-065, blueprint 6.1–6.4): the latest version's probabilities as percentages totalling 100.0, the framing sentence (never "will win"), expected goals, most likely scorelines, leading factors with who they favour and why, then version, kind, computation time, model id and data completeness; "What changed between versions" in percentage points; the post-match evaluation once the match is over. An `unavailable` version shows its reason; no forecast says so. |
| `src/lib/forecast.ts` | Percentages that total exactly 100.0, favourite and margin, the framing sentence, version-to-version deltas and their wording, labels for kinds, factors and unavailable reasons (`forecast.spec.ts`). |
| `src/components/prediction-section.tsx`, `src/components/prediction-form.tsx` | "Your prediction" on the match centre (T-050): guests are told to sign in, an unverified member to verify, an open match gets the form (outcome, optional exact score, confidence, up to three reason tags, a short explanation; resubmitting is a new version and the form says which version it is), a locked match shows the final version read-only with its time. |
| `src/lib/prediction-actions.ts`, `src/lib/prediction-form.ts` | The server action that PUTs the form to the API and returns its field errors unchanged; the pure shaping of the form into the request, tag and outcome labels, the kick-off lock (`prediction-form.spec.ts`). |
| `src/lib/match.ts` | Labels for incidents, statistics and coverage states, minute formatting, and the blueprint 4.2 modules the page does not have yet (`match.spec.ts`). |
| `src/app/[locale]/u/[username]/page.tsx` | A member's profile as the API allows this viewer to see it: full, or name-only when restricted. T-056: the Performance Rating (rating, tier, status, sample, formula version) and the prediction history (`?page=`), each with its own restricted / unreachable states. T-202: `FriendControls` on both branches, because a friends-only profile the viewer cannot see is exactly the one they may want to ask about. |
| `src/app/[locale]/messages/page.tsx`, `messages/[id]/page.tsx` | Conversations on the web (blueprint 8.3, T-224): the list, and one conversation with its messages, composer, search, paging back, mute and leave. **Server components with no socket** -- T-230 adds the transport beside this, not instead of it. Reading the page is what moves the read position, so "read" means "opened". |
| `src/components/conversation.tsx` | A message and a shared football card, rendered as they are *now*: the fixture card carries its `last_updated_at` (rule 4) and its score goes through `<Score>` so right-to-left cannot reverse it (T-153). Every card kind is an explicit branch and the last statement says it cannot show an unknown one -- a fall-through would have labelled a new kind somebody's prediction. `conversation.spec.ts` reads `CARD_KINDS` from the contract and checks the exits and the server-rendered composer. |
| `src/components/live-conversation.tsx` | The chat page's socket (T-237). It **renders no message**: when something arrives it calls `router.refresh()` and the server lays the conversation out again, so there is never a second way a message can look and no client-side copy to fall out of step with the store. Subscribes with the page's `latest_seq`, so a reconnect brings back what was missed (T-235). Says which of the two it is -- live, or not live and reload -- because a socket that quietly stopped delivering while the page looked live would be rule 4 broken on a surface with no timestamp. A hidden tab holds the refresh until it is visible again: opening the page is what marks it read, and a tab nobody is looking at must not claim that. |
| `src/components/health-panel.tsx` | The score stream and the chat layer on the administration page (T-236), beside the ingestion block that has been there since T-070. Unreachable is **stated** rather than rendered as zero -- "no connections" and "we could not ask" are different facts. Every chat number says it belongs to one instance, because sockets live on the process that accepted them. `latency_ms` of `null` reads as "nothing delivered yet", never as 0 ms. `health-panel.spec.ts` reads `ChatBusState` from the contract, so a state with no words of its own is a failing test rather than a fall-through. |
| `src/lib/language-coverage.ts`, `src/components/language-coverage.tsx` | The operator's language table on `/admin` (T-302): `languageRows()` is one row per real locale with the translators' numbers through `coverage()`, the percent rounded down, and `offered` from `isShippable` -- the same test the picker (T-306) uses, so what the operator reads as not yet offered is exactly what a reader is not shown. It names the eight `language.name.*` keys literally, which is what gives them a surface. The component renders the rows and computes nothing; `language-coverage.spec.ts` checks both, and reads the component's source rather than importing it because vitest does not transform `.tsx` here. |
| `src/components/group-controls.tsx` | What a member can do about a group (T-242), following `GroupStanding` exactly -- **every standing is a branch**, with an honest unknown fallback below them, so a standing the contract adds and this file forgets is a failing test rather than a page that quietly offers nothing. An invite-only group offers **nothing to press**, because a control that would always be refused is a worse answer than saying how the group is joined; `unavailable` never says why. One form per control over a server action, no script: joining and leaving are what a member reaches for when they want into or out of something. `group-controls.spec.ts` reads `GROUP_STANDINGS` from the contract and checks the stated absences on both pages. |
| `src/app/[locale]/groups/[slug]/page.tsx` *(board section, T-243)* | The group's board, fetched only where `members` came back non-null -- the API's own answer to who may see the membership, asked once. Labels come from `@/lib/leaderboard`, so there is no second rating format, tier table or status word; an empty board states the filter that emptied it rather than rendering a list of nobody. |
| `src/components/conversation-controls.tsx`, `src/lib/conversation-actions.ts` | The client half and its server actions: send, remove your own message, mute, leave, and "Message" from a friend's profile. Every refusal is the API's, including the ones that do not say why. T-226 adds `Reactions` and `PinMessage`: a form per reaction so reacting works with no JavaScript, the unused ones behind a `<details>` rather than a scripted popover. |
| `src/app/[locale]/friends/page.tsx` | Friends, requests and blocks (blueprint 8.1, T-202). Three sections, each stating its own absence; the block list lives here rather than in settings, because a block a member cannot find is a block they cannot lift, and it says that lifting one does not restore the friendship it ended. |
| `src/components/friend-controls.tsx` | The control set for one member, driven by `FriendStatus`: add, cancel, accept, decline, remove, block, unblock — one form each, so every control works without JavaScript and a failure shows beside the thing that failed. `unavailable` says a request cannot be sent and not why (see the file for the three options and why this one lies to nobody). The block control is present in every state, including that one. `friend-controls.spec.ts` is the exits guard (D-053): it reads `FriendStatus` out of the contract and fails if any state renders without a way out. |
| `src/lib/friend-actions.ts` | The server actions behind those controls. Nothing is re-implemented: every refusal, including the wording that does not reveal a block, is the API's. |
| `src/app/[locale]/u/[username]/compare/page.tsx` | Two members' ratings and prediction records side by side (blueprint 8.1, T-203). It adds no access: the history comes from the endpoint the profile uses, so a restricted history is restricted here and the page says which. |
| `src/lib/compare.ts` | The comparison's arithmetic, pure: the matches both predicted, the four-way tally over settled matches only, and `truncated()`, which is why the page says when it is looking at a window rather than a career. `compare.spec.ts` argues with every exclusion. |
| `src/components/prediction-history.tsx` | The history list (T-056): per match the fixture with its score, the version that stands as submitted, its time in the viewer's zone, how it settled, earlier versions behind a disclosure, paging. |
| `src/lib/prediction-history.ts` | The history's pure helpers: paging, version / settlement / fixture labels, the submission time in a zone. `prediction-history.spec.ts` covers them. |
| `src/app/[locale]/founder/[fixtureId]/page.tsx` | Writing the founder's analysis (T-131): a plain server-action form of the blueprint's sections, the published versions listed newest first, and the kick-off wall shown instead of a form once a match has started. Re-implements no rule — the field errors are the API's. Never indexed. |
| `src/app/[locale]/admin/page.tsx` | The administration area (T-070): ingestion, freshness, coverage with the audited coverage form, the rating rules in force, member search with the audited status form, the audit log. 401 → sign-in, 403 → not found. |
| `src/lib/admin-actions.ts` | The two audited server actions: account status and declared coverage, each carrying a reason. |
| `src/app/[locale]/settings/page.tsx` | Profile and privacy forms for the signed-in member; redirects to login otherwise. |
| `src/components/site-header.tsx` | The navigation bar (Scores, Leaderboard, the search box, account). Reads the session server-side; shows the visitor as signed out when the API cannot be reached. |
| `src/components/action-form.tsx` | The one form component: fields from a spec, errors from the API's `ApiError.fields` through `useActionState`. |
| `src/components/following-section.tsx` | Follows and favourites on the settings page: pin, unpin, unfollow, and pickers over `GET /teams` and `GET /competitions`. Plain forms over server actions. |
| `src/lib/auth-actions.ts` | Server actions for every account form. They call the API, mirror its session cookie onto the web origin (D-027), and redirect or return an `ActionState`. |
| `src/lib/session.ts`, `src/lib/set-cookie.ts` | Forwarding the visitor's session cookie to the API and mirroring the API's `Set-Cookie` back. The parser is pure and tested. |
| `src/app/globals.css` | Tailwind entry point and global styles. |
| `src/i18n/locales.ts` | Which locales ship, the pseudo-locales, the unfinished ones and the writing direction of each. All eight of blueprint 13's languages route (`en` plus `ar`, `de`, `es`, `fr`, `it`, `pt`, `tr` -- T-150, T-300); the seven non-English ones are unfinished. An unfinished locale routes and renders so a translator can see the work in place, and is kept out of the index and out of any language list until its catalogue is done. `locales.spec.ts` asserts the eight by equality, so a locale added or dropped without a decision fails. |
| `src/i18n/messages.ts` | The message catalogue and the missing-string policy (T-151): `en.json` is the source of truth, `message()` returns the text *and* whether it is `source`, `translated` or `untranslated`, and `completeness`/`isShippable` decide when a locale may be offered as a language. Reads the translators' files in `catalogues/` and holds no words of its own (T-302, D-066); `coverage(locale)` is the product's answer to how far a language has got. `attribute()` is for text that goes in a `placeholder` or an `aria-label`, where `Translated` cannot wrap a fallback in a marked span: the `lang` moves onto the element holding the attribute. `messages.spec.ts` fails on a catalogue key with no call site, on a translator's file that is stale or claims a status its text does not support, and holds the keys whose surface is not built yet in a list that may only get shorter. |
| `src/lib/language-picker.ts`, `src/components/language-picker.tsx` | The language picker (T-306): `offeredLocales` is every real locale `isShippable` says is done, `pickerEntries` is `[]` below two of them and otherwise one entry per language -- its own name from its own catalogue, the same page under that locale, which one is current -- and the client component renders exactly that in the header, or nothing. Nothing today, on purpose and under test; the day a second catalogue crosses the threshold it appears without a deployment decision. The predicate is injected so `language-picker.spec.ts` shows both states without pretending a catalogue is finished. |
| `src/i18n/messages.ts` | The message catalogue and the missing-string policy (T-151): `en.json` is the source of truth, `message()` returns the text *and* whether it is `source`, `translated` or `untranslated`, and `completeness`/`isShippable` decide when a locale may be offered as a language. Reads the translators' files in `catalogues/` and holds no words of its own (T-302, D-066); `coverage(locale)` is the product's answer to how far a language has got. A key whose English is an object of forms is a plural (T-301, D-067): `plural()` selects the form by `Intl.PluralRules` for the locale, cardinal or ordinal, fills `{count}` in the locale's digits, and falls back to the English forms by English rules; `message()` answers a plural key with its `other` form so nothing is ever blank, and pages render plurals through `Translated` with a `count`. `attribute()` is for text that goes in a `placeholder` or an `aria-label`, where `Translated` cannot wrap a fallback in a marked span: the `lang` moves onto the element holding the attribute. `messages.spec.ts` fails on a catalogue key with no call site, on a translator's file that is stale or claims a status its text does not support, and holds the keys whose surface is not built yet in a list that may only get shorter. |
| `src/i18n/catalogues/` | The translators' files (T-302, D-066). `en.json` is every message the product shows; each other locale's file carries every one of those keys as `{ source, text, status, note? }` for a fluent speaker to edit directly. Never machine-filled. |
| `scripts/i18n-catalogues.mjs` | Keeps the translators' files in step with `en.json` (`i18n:catalogues`; `--check` in `i18n:catalogues:check`): adds new keys as `untranslated`, refreshes every `source`, keeps every translation as it was, refuses to drop a key that still carries words, and refuses a translated plural whose forms are not exactly the categories `Intl.PluralRules` says its language has. |
| `src/i18n/format.ts` | Dates, times and numbers in the reader's language (T-300): `formatDateTime`, `formatTime`, `formatDate`, `formatNumber`, and `intlLocale`, which maps `en` and the pseudo-locale to `en-GB` so an English page formats byte-for-byte as it did before the module existed. Clock readings (`formatTime`) are fixed on the 24-hour cycle; a date-and-time follows the locale. The pseudo-locale maps to English because `Intl` throws on a private-use tag. It is for dates a person reads; the two machine formats in `lib/scores.ts` (time-zone validation on `en-US`, the day-tab key on `en-CA`) stay where they are, and `format.spec.ts` fails if anybody routes them through here. |
| `src/lib/api.ts`, `src/lib/locale-query.ts` | Every call to `apps/api`, server-side only, typed by `@fmip/contracts`. The team, competition and player fetchers take the reader's locale and append `?locale=` through `withLocale` (T-303), so the API answers the localised name beside the canonical one. Failure is a value (`status` 0 = unreachable), never a throw. Sends a `web-<uuid>` request id with every call (T-071). |
| `tests/e2e/journeys/journeys.spec.ts` | The blueprint journeys (T-080, D-043) through the whole stack: 18.1 register → verify from the real mail → predict → history, 18.2 the live card, 18.3 eligibility without points, 18.4 follow → pinned → team → match → player → search. Runs only in the `journeys` project (`E2E_API_URL`). |
| `tests/e2e/journeys/members.ts` | Making a member the way a member is actually made: register through the real form, verify from the link in the API's own mail log (D-026). Shared because one spec needs one member and one needs two, and a second copy of it would drift. |
| `tests/e2e/journeys/news.spec.ts` | The news page over the seed, which carries no feed (T-143): every section says what it holds and why, a guest's following asks for a session, a filter that matches nothing says so and clears. |
| `tests/e2e/journeys/social.spec.ts` | Blueprint 19's social list with **two** members, which is the only way any of it can be seen: a friend request reaching its recipient's own page, a conversation holding its order for both sides, a message arriving over the socket with nobody reloading, and a block that is neither symmetrical nor announced. These are the Phase 3 exit criteria the public preview could not walk, and the socket one it could not have walked at all. |
| `tests/e2e/a11y.spec.ts` | The accessibility pass (T-081, D-041): axe-core WCAG 2.2 AA on every kind of page, the keyboard path through the skip link, the live region. |
| `tests/e2e/pwa.spec.ts` | Installability (T-082): the manifest and its icons, the page links, the service worker registering and serving the offline page with the network off. |
| `tests/e2e/seo.spec.ts` | The SEO surface with JavaScript disabled (T-039): content, canonical, alternates, robots rules, JSON-LD, `/robots.txt` and `/sitemap.xml` straight from the rendered HTML. |
| `tests/e2e/rtl.spec.ts` | The RTL check. Asserts computed layout, never screenshots. |
| `tests/e2e/match.spec.ts` | The match centre page without an API: a malformed id is a 404 page, the unreachable notice, the stream proxy's 503 and 404. |
| `tests/e2e/scores.spec.ts` | The scores page without an API: the strip, the filters, state kept in links, the unreachable notice, the RTL mirror. |
| `playwright.config.ts` | Runs the E2E suite against a production build: the `chromium` project (the web app alone) always, the `journeys` project when `E2E_API_URL` is set (D-043). |
| `Dockerfile` | Multi-stage build on Next's standalone output. Built from the repository root. `next build` leaves `static` **and `public`** out of the standalone bundle, so both are copied in by hand and the image then asserts `public/sw.js` and `public/icons` are there: without them the service worker and every icon answer 404, the browser stops offering to install the app, and nothing local notices, because `next start` reads `public` from the working tree (T-082). |
| `src/proxy.ts` | Redirects any path without a locale segment to the default locale. |
| `stylelint.config.mjs` | Bans physical CSS properties (rule 7). |
| `eslint.config.mjs` | Bans physical Tailwind utilities in `className`, plus Next's rules. |

**RTL safety is enforced in two places, because layout lives in two places.**
Stylelint rejects `margin-left` and friends in CSS; ESLint rejects `ml-*`,
`text-left`, `border-l-*` and friends inside a `className`. In a Tailwind
codebase almost all layout is class names, so a CSS-only rule would cover
almost nothing. Both fail the build, not just review.

The locale segment is the only routing rule: every page lives under one, and
`src/proxy.ts` redirects anything that arrives without it. A URL therefore
always says which language it is in.

**`x-rtl` is a pseudo-locale, not a language.** It serves the same English text
in a right-to-left document, so a physical-property regression becomes visible
before anyone ships Arabic. It is routable in every environment — the check runs
against a production build — and carries `noindex`, because duplicate English
under a second URL is an SEO problem on a product that depends on search.

The `<h1>` accent bar is the canary: `border-s-4 ps-4` must render on the left
in `en` and on the right in `x-rtl`. Written as `border-l-4 pl-4` instead, `/en`
still looks perfect and only the `x-rtl` assertion fails — which is the whole
argument for having the pseudo-locale.

## `packages/`

| Path | Purpose | Consumed by |
|---|---|---|
| `packages/contracts` | API request/response types, shared enums, coverage states. **The single source of truth for the API shape.** | `apps/web`, `apps/api` `platform-independent.spec.ts` (T-321) is the guard that keeps it belonging to no platform: shipped modules import only from themselves, no runtime or peer dependencies, no browser or Node global named in any source, `ES2023` with no `DOM` and no `types`, and the specs excluded from the build. Phase 4's second client rests on this being true; it is checked rather than assumed. |
| `packages/ingestion` | The normalised model adapters produce, the adapter contract, and the recorded-fixture harness that verifies an adapter, and the three adapters: `api-football` (T-021), `football-data-org` (T-022), `highlightly` (T-023). | `apps/api` |
| `packages/db` | Schema, migrations, seed data. Plain SQL, applied by node-pg-migrate (D-022). `Dockerfile` builds the production `migrate` tool image (T-074). | `apps/api` |
| schema `training` (was `packages/db/training`) | Historical datasets for model training only, as a Postgres schema created by the T-060 migration (D-028). **Never read by `apps/api` or `apps/web`** (D-014) | `apps/model` |
| `packages/ui` *(planned)* | Shared React components, design tokens, RTL-safe primitives | `apps/web` |
| `packages/config` | Shared tsconfig, eslint, prettier. Published as `@fmip/config`. | everything |

### `packages/config`

Consumed by every other workspace. Nothing here imports from anywhere else.

| Path | Purpose |
|---|---|
| `tsconfig/base.json` | Strictness baseline. Every other preset extends it. |
| `tsconfig/library.json` | For `packages/*`: composite build. Each project sets its own `rootDir`/`outDir`. |
| `tsconfig/nestjs.json` | For `apps/api`: CommonJS + decorator metadata. |
| `tsconfig/nextjs.json` | For `apps/web`: bundler resolution, JSX, `noEmit`. |
| `eslint/base.js` | Flat config. App configs spread it and add their own layers. |
| `prettier/index.js` | Formatting options. |
| `tests/shared-config.spec.ts` | Guards the invariants above against silent drift. |

### `packages/contracts`

| Path | Purpose |
|---|---|
| `src/coverage.ts` | `CoverageState`, the `Covered<T>` envelope, and `hasData`. |
| `src/health.ts` | The `GET /health` response shape. |
| `src/following-feed.ts` | The Following feed (T-333): `FollowingFeed` with `showing` (window, kinds, what is followed), `ranking` (`feed-rank@1` and its signal names -- no `views`), and `FeedItem`s of four kinds, each with a `because` list of `FeedSignal`s and the `rank` they add up to (a match's `score` stays a score). |
| `src/territory.ts` | The viewing territory (T-312): `Territory` (ISO 3166-1 alpha-2 and a name), `ViewingTerritory` as a state -- `chosen` with the territory or `not_chosen` -- so a surface has to write the sentence for the second case, `GET /territories`, `GET`/`PUT /me/territory`. |
| `src/notifications.ts` | In-product notifications (T-270) and, from T-331, `NOTIFICATION_CATEGORY_OF` (every kind in exactly one of `football`, `social`, `account`), `MuteScope`, `NotificationMute` and `NotificationSettings.mutes`. |
| `src/viewing.ts` | Watch and highlights (T-311): `ViewingOption` (broadcaster, access, official destination, territory, source and what it grants), `Highlight` (embed or official page, per territory), and `MatchViewing` -- the viewer's territory as a state plus `options` and `highlights` as `Covered` lists, so a surface cannot render an empty panel that reads as either "not supplied" or "not available" without saying which. T-313 adds the desk's request and response types (`ViewingCoverageRequest`, `ViewingOptionRequest`, `HighlightRequest`, `ViewingRemovalRequest`, `ViewingBatchResponse`) and an `id` on each listing and highlight. |
| `src/intelligence.ts` | The intelligence layer (Phase 5, D-070): `IntelligenceHealth` (the model this deployment can drive, or none), and `MachineText` -- the labelled shape of anything a machine wrote: the text, its language, the model, the prompt version and when. |
| `src/news.ts` | The news sections (T-143): `NewsSectionResponse` with `stories: Covered<NewsStoryCard[]>` and a `reason` for what the section holds or why it holds nothing; a card is a story's promoted original with the publisher's words, rights and link, its entities by id, and `discussion` (trending) or `debate` (debate) beside it. `StoryPage` (T-144) is blueprint 3.3's page under D-061: the card, its versions, corrections, the other reports, and `body: Covered<string>`, `not_supplied` for every free feed. |
| `src/index.ts` | The package's whole public surface. |

**Every module payload the API returns is a `Covered<T>`**, carrying a coverage
state and a `last_updated_at`. That is rules 3 and 4 expressed as a type rather
than a convention: a caller cannot read the data without having been handed the
coverage state next to it.

Changing a type here breaks `apps/api` and `apps/web` in the same build, which
is the point of the package (D-006).

### `packages/ingestion`

| Path | Purpose |
|---|---|
| `src/normalised.ts` | The provider-neutral shapes every adapter returns: fixture, incident, lineup, standing, statistic, period, and the closed value lists they share with the schema. Entities carry the provider's id plus a name; unsupplied fields are `null`, never zero or a guess. |
| `src/news/feed.ts` | A publisher's feed -> `NormalisedNewsItem[]` (T-142, D-061): RSS 2.0 and Atom read by hand over the shared `Transport`, taking only headline, the publisher's summary, byline, time and the link -- the type has no body field, so nothing can be taken that a free source does not grant. Missing fields are `null`, never guessed; an item without a headline or link is skipped and counted; a non-feed answer is `malformed`, not empty. `feed.spec.ts` uses format samples written for the spec, which are a specification of the reader, not a recording of anybody's feed. |
| `src/adapters/_contract.ts` | `ProviderAdapter` (five calls: fixtures, live, lineup, standings, detail), `AdapterManifest` (licence, tier, quota, critical-path flag), `Transport` (the only way to the network, injected), `AdapterResult` (failure is a value, with the request count). |
| `src/adapters/_fixtures/` | Recorded provider responses, one directory per provider, one JSON scenario per call. Recorded with `RecordingTransport` through `scripts/record.mjs`, never hand-written. `api-football/`, `football-data-org/` and `highlightly/`: six scenarios each from 2026-09-10, the same Premier League 2023/24 fixtures (T-021..T-023). |
| `src/adapters/api-football/` | T-021. `index.ts`: `createApiFootballAdapter` and `API_FOOTBALL_MANIFEST` (licensed API, free tier, 100/day, 10/min); one request per call; the v3 envelope's `errors` object becomes `quota` / `unsupported` / `http` / `malformed`. `map.ts`: the only file that knows API-Football's field names — statuses, rounds → stage kinds, events → incidents (side by team id, substitute as the related player, unattributed events dropped), lineups matched to home/away by team id with captains from the per-player block, statistics by name (`55%` → 55), standings per group. Live state is `fixtures?live=all` filtered to the requested ids: the free plan refuses `ids=`. `api-football.spec.ts`: the contract check over the recordings plus mapping-rule tests. |
| `src/adapters/football-data-org/` | T-022. Same shape as `api-football`, against v4 on the free tier (TIER_ONE, 10/min). `map.ts`: statuses, `REGULAR_SEASON`/`GROUP_STAGE`/`LAST_16`… → stage kinds, `matchday` → round, the ninety-minute score from `regularTime` when a match went further (null when not split), `form` reversed because the provider writes it newest first (verified live), goals/bookings/substitutions → incidents and lineups for the tiers that carry them. On the free tier lineups are `unsupported` and a detail has no incidents or statistics. `/matches?ids=` serves the live call. |
| `src/adapters/highlightly/` | T-023. Same shape, against `sports.highlightly.net/football` on the BASIC plan (100/day) with the RapidAPI-style headers the direct API insists on. Request cost is different and reported honestly: a fixture list is one request per day of the range (refused beyond 14), the live call one request per fixture (refused beyond 20; there is no batch lookup). `map.ts`: free-text `state.description` → status by keyword, string scores (`0 - 3`), event times (`90+4`), events with the substitute as the related player, statistics by display name with possession fractions → percentages, standings per group without form. No half-time scores exist; a lineup with no starters is `unsupported`; an unknown id answers an empty array and is reported as `malformed`. |
| `src/bakeoff/` | T-024. `metrics.ts`: field completeness per module (optional normalised fields filled / could have been filled), cross-provider matching by kick-off and normalised team name, disagreements (same match, different status or score, counted only where two providers supplied the field). `run.ts`: `runLive` (a `TimedTransport` around `fetch`, paced to each manifest's per-minute quota, response times, every provider over the same competitions and dates) and `runRecorded` (the `_fixtures` scenarios replayed; deterministic, no network); both produce the same `BakeoffResult`. `report.ts`: the Markdown results table and `insertResults`, which replaces the block between `<!-- bakeoff:start -->` and `<!-- bakeoff:end -->` in `docs/05-data-providers.md`. Goal latency, lineup lead time and lineup accuracy are listed as not measured: they need the polling job (T-026). |
| `scripts/bakeoff.mjs`, `scripts/plans/bakeoff.mjs` | `node scripts/bakeoff.mjs --recorded` or `--live` (keys from the environment; a provider without a key is skipped and named). Writes the table into `docs/05-data-providers.md` and the full result to `bakeoff/<timestamp>-<mode>.json`. The plan is the fixture set: five 2023/24 opening weekends, the seasons every free plan serves. |
| `scripts/record.mjs`, `scripts/plans/<provider>.mjs` | Records a provider's scenarios from the live API (`node scripts/record.mjs api-football [--only=name]` after `pnpm build`): the plan lists calls and arguments, the expectation is derived from the adapter's own result, the key is read from the environment and the output is scanned for it. |
| `src/harness/contract-check.ts` | `checkAdapterContract(factory, scenarios)`: replays each scenario through `ReplayTransport`, checks the manifest (D-014), that failure is returned not thrown, that no unrecorded URL was requested, that the request count is honest, and that success validates as the normalised shape. Returns problems; empty means pass. `loadScenarios(dir)` reads a provider's recordings. |
| `src/adapters/replay/` | T-026, D-049. Not a provider: `createReplayAdapter(provider)` builds a real adapter over a `ReplayTransport` fed with every request in that provider's committed recordings, so a job runs the real mapping and the real writers with no key and no network. `replayAvailable` says whether the recordings are in this build — they live under `src/`, which `dist` does not ship, so a deployment without them reports "no source" instead of failing. `replay.spec.ts` proves each of the five calls answers from a recording. |
| `src/harness/replay-transport.ts` | `ReplayTransport` answers only from recordings and lists what it could not answer; `RecordingTransport` performs real requests and remembers them, for producing recordings. |
| `src/harness/validate.ts` | Hand-written validators for each normalised shape and the manifest, restating the schema's rules at the boundary (statuses, ranges, uniqueness, "finished needs a full-time score", one captain, D-014). |
| `src/harness/contract-check.spec.ts` | The harness's own proof: a conforming fake adapter passes; deliberately broken ones (leaked provider status, faked coverage, unrecorded URL, thrown error, misreported requests, scraped critical path, success where failure was recorded) each fail by name. |

**An adapter is verified by its recordings, not by its author.** `checkAdapterContract`
on an adapter with no scenarios reports it as unverified rather than passing it.
The adapter never reaches for `fetch`: it is constructed with a `Transport`, so
the same code runs against the recording in tests and against the provider in
the bake-off, and the request count it reports is checked against the transport.

`apps/api` will import `PROVIDERS` and the entity-type list from this package
once an adapter exists (T-021); until then the resolver in
`apps/api/src/modules/ingestion/` carries its own copy of both lists.

### `packages/db`

| Path | Purpose |
|---|---|
| `migrations/*.sql` | One file per change, `<timestamp>_<slug>.sql`, each with an up and a down section. Never edit a shipped one. `..._bootstrap.sql` is the version gate and the `set_updated_at()` trigger function; `..._catalog.sql` is T-010: `country`, `competition`, `season`, `stage`, `venue`, `team`, `person`, `player_spell`. `..._fixtures.sql` is T-011: `fixture`, `fixture_participant`, `fixture_score`, `fixture_period`, `incident`, `lineup`, `fixture_stat`. `..._ingestion.sql` is T-012: `provider_mapping`, `coverage_profile`, `ingest_run`. `..._unresolved-entity.sql` is T-013: the review queue for provider ids that do not resolve. `..._identity.sql` is T-040: `user_account`, `credential`, `session`, `email_token`, `user_role`. `..._profile.sql` is T-041: `profile`, `privacy_setting`. `..._followed-entity.sql` is T-042: `followed_entity`. `..._training-store.sql` is T-060: schema `training` with `source_load`, `match`, `elo`. `..._training-team-alias.sql` is T-063: `training.team_alias`, catalog team → training name per division. `..._forecast.sql` is T-064: `competition.football_data_division`, `model_version`, `input_snapshot`, `forecast`, and the `refuse_change()` trigger that makes snapshots and forecasts immutable (rule 5). `..._evaluation.sql` is T-066: `evaluation`, one immutable row per forecast version scored against the full-time score. `..._fixture-change-notify.sql` is T-032: `notify_fixture_change()` and the triggers that raise `NOTIFY fixture_change` after any write to a fixture or what hangs off it (D-034). `..._predictions.sql` is T-050: `user_prediction` and the immutable `prediction_version`. `..._prediction-lock.sql` is T-051: `refuse_prediction_after_kickoff()`, the trigger that refuses a version once the database clock has reached kick-off (SQLSTATE `PL001`). `..._settlement.sql` is T-052: `settlement_run` and the immutable `settlement` (settled against the full-time score, or void with a reason; one settled row per prediction). `..._rating.sql` is T-053: the immutable `rating_snapshot` (formula version, components, inputs hash). `..._career-points.sql` is T-054: the immutable `points_transaction` ledger, one row per settlement and reason. `..._search.sql` is T-038: `pg_trgm` and `unaccent`, `search_key(text)`, `entity_alias`, trigram indexes on names and aliases (D-039). `..._localised-names.sql` is T-303: `kind = 'name'` on the same table for an entity's display name in a language -- a name must have a language, one per language per entity by a partial unique index, and `localised_name(type, id, language)` answers the row or NULL, never the English. `..._panel-post-notifies.sql` is T-254: `panel_post` raises `fixture_change` like a goal, so the match page's stream can say the discussion moved. `..._news-schema.sql` is T-141 (D-061): `news_source` with its `rights`, `story`, `article`, `article_version` (immutable, one per language), `article_entity` (by UUID, no name column) and `article_correction`; `article_version_within_rights()` refuses a version that carries more than its source grants (SQLSTATE `PL016`). `..._news-fetch.sql` is T-142: `news_fetch`, one open run per source, and `article_version.published_at` made nullable because a publisher who gives no time gives no time. `..._story-debate.sql` is T-143: `story_debate`, an editor's selection of a story for the debate section with who and why, cleared by whom and why, one open selection per story -- a row rather than a flag, because a flag has no history and no author. `..._viewing-territory.sql` is T-312: `territory` (the full ISO 3166-1 list, seeded, because rights are sold by state and a football country is not always one) and `user_account.viewing_territory`, nullable and null for everybody until they choose -- never inferred, and `country_id` never read in its place. `..._viewing.sql` is T-311 (blueprint 11): `viewing_source` with its `rights` (`link`, `thumbnail`, `embed`), `broadcaster`, `viewing_option` (one listing per match per territory per service, with the access type and the official destination), `highlight` (one per match per territory, an embed only from a source that grants one -- `PL017` -- and always the official page beside it) and `viewing_coverage` (per season, territory and module; a supplied state names its source), plus the trigger that makes a dropped source take its rows and turn its coverage into `not_supplied` with the reason. The spec is `apps/api/src/modules/viewing/viewing.schema.spec.ts`; there is no module code until T-313. `..._article-translations.sql` is T-304: `article_version.origin` (`publisher` or `translation`), the catalogue's review state (`translated`, `reviewed`) with `written_by` and `reviewed_by`, and the constraints that make a translation a person's and a review a second person's. `..._notification-mutes.sql` is T-331: `notification_mute` (a team or competition by id, or a category; the shape checked before any cast), `notification_about()` (the teams and competition behind a fixture, prediction or panel-post subject) and `notification_muted_for()`, the one question emission asks -- so a team mute silences what is about that team's matches and nothing else. `..._audit-log.sql` is T-070: the immutable `audit_log` (rule 10). `..._power-index.sql` is T-110: the immutable `power_index`, one row per team per fixture per formula version per moment, with the components and the weights they carried and the share of the formula's weight actually supplied.  `..._founder-analysis.sql` is T-130: `founder_analysis` (one per fixture, with its author) and the immutable `founder_analysis_version`, locked at kick-off by `refuse_analysis_after_kickoff()` (SQLSTATE `PL002`) so an analysis can never be revised once the result is known.  `..._arabic-search-key.sql` is T-152: `search_key` also folds Arabic-script letter variants and digits and drops tatweel and harakat, with the six indexes built on it rebuilt.  `..._social-graph.sql` is T-200: `friendship` as one unordered pair (`low_id < high_id` by CHECK), `friend_request` as an open offer with no history of refusals, `user_block`, the `users_blocked(a, b)` function that is the single definition of "these two must not reach each other", and the triggers that refuse a request or a friendship across a block (SQLSTATE `PL003`) and end the friendship when one is created.  `..._moderation.sql` is T-210: `report` (one open per reporter per subject, open exactly while `decision_id` is null), the immutable `moderation_decision`, `sanction` (which ends or declares itself permanent, and records who lifted it and why), the immutable `appeal_note`, the `member_sanctioned(user, scope)` function, and the trigger that refuses a friend request from a member under an active `contact` sanction (SQLSTATE `PL004`). Every CHECK list is short on purpose: a scope nothing enforces would tell the moderation team a restriction was in force.  `..._rate-limit.sql` is T-213: `rate_limit` (the ceilings, as rows an administrator can UPDATE), `rate_window` (one fixed hour per member per action), and `refuse_over_rate(action, actor_column)`, whose trigger is named `friend_request_volume_guard` so it fires after the block and sanction guards -- a member who is both restricted and over the ceiling hears about the restriction, which is the one they can appeal. SQLSTATE `PL005`, served as 429.  `..._group-conversations.sql` is T-245: `conversation.kind` gains `group`, `conversation.group_id` arrives with the same shape the pair has, a unique index gives each group exactly one conversation, and `refuse_non_participant()` is replaced by one that asks `group_member` for a group and `conversation_participant` for a direct conversation -- asking the place the membership is actually kept is what makes a change to it immediate (D-058).  `..._groups.sql` is T-240: `user_group` (three visibilities, because discoverable-private -- found but not read -- is the case a boolean would lose; the slug never changes, `PL008`), `group_member` (exactly one owner: a partial unique index for at most one, a **deferred** constraint trigger for at least one, `PL009`, deferred so that handing a group over -- demote then promote -- is one legal transaction), `group_invite` (no status column, the same shape as `friend_request`) and `group_join_request`. How you get in follows from the visibility (D-057): join a public one, ask a discoverable one, and asking an invite-only or a public one is refused (`PL011`) rather than filtered. An invitation answers the same questions a friend request does -- a block (`PL003`), a contact sanction (`PL004`), already a member (`PL010`). A `groups` sanction stops making and joining and nothing else. Widens `sanction.scope` with `groups` and both `subject_type` lists with `group`.  `..._reactions-mentions-pins.sql` is T-225: `message_reaction` (a closed set of six), `message_mention` (resolved once on write, refused across a block with `PL003`) and `conversation_pin` (a row, so a message keeps its one permitted UPDATE), plus the trigger that drops the reactions and the pin when a message is tombstoned.  `..._message-search.sql` is T-223: a partial GIN trigram index over `search_key(body)` for messages that still stand.  `..._message-cards.sql` is T-222: `message.card_kind` and `card_id` (one of four tables by kind, no foreign key, the same shape as `report.subject_id`), a body that becomes optional when a card is present, and a tombstone that drops the card.  `..._conversations.sql` is T-220: `conversation` (the ordered pair for a direct one, at most once, plus its `next_seq` counter), `conversation_participant` (joined, left, muted, `last_read_seq`) and `message`. Five BEFORE INSERT triggers named for their firing order -- `access` (`PL006`), `block` (`PL003`), `sanction` (`PL004`), `volume` (`PL005`), `write_sequence` -- so every refusal happens before a sequence number is spent; `message_no_rewrite` (`PL007`) allows only the tombstone UPDATE and no DELETE. It widens `sanction.scope` with `messaging` and the two `subject_type` lists with `message`, in the migration that builds the surface they cover.  `..._group-threads.sql` is T-244: `conversation.kind` gains `group_thread`, with `fixture_id` and `opened_by`; `conversation_kind_has_its_columns` replaces the T-245 shape CHECK and spells all three kinds out; `conversation_one_per_group` is **narrowed to `kind = 'group'`** (left alone it would have refused a group its first thread with a duplicate-key error nobody could read), and one thread per fixture per group gets an index of its own. `refuse_non_participant()` widens by one word, so a thread's membership is the group's for the same reason the group room's is. `refuse_thread_from_outside()` (`PL012`) refuses a thread opened by somebody outside the group -- the guard, not the courtesy, because such a thread would be a room its opener could then write in. Both BEFORE triggers step aside for a malformed row so the shape CHECK is the one that answers it. Ceiling `group_thread`, 20 an hour.  `..._contributor-grant.sql` is T-250: `member_under_sanction(user)` (any scope, because a conduct check that named its scopes would stop covering the ones added after it), the `contributor_eligibility_input` **view** -- a view because it has no INSERT path, so "eligibility never grants" is the shape rather than a rule, and carrying the facts and not the verdict because the thresholds are configuration and live in `ELIGIBILITY_V1` -- plus the immutable `contributor_grant` (approver, reason, and which contributor rules were accepted) and `contributor_grant_event` (paused, resumed, withdrawn, each with an actor and a reason). `contributor_grant_standing()` derives the standing from the newest event, tie-broken by a sequence because two events in one transaction share `now()` exactly; `member_may_contribute()` is what T-251 asks, and asks about the grant only -- a rating that dips must not undo a decision somebody signed. `PL013` refuses an event on a withdrawn grant, a second pause, a resume of something not paused, and a second live grant. Self-approval is deliberately **not** refused: the row names the approver twice, and at launch the founder may be the only person who can grant anything.  `..._match-panel.sql` is T-251: `panel_post`, shaped like `message` (the same tombstone, no edit, `PL007`) but with **no `seq`** -- a panel is read rather than subscribed to, and a counter nobody reads is a column to keep correct for nothing. Three BEFORE INSERT guards named for their firing order -- `approval` (`PL014`, `member_may_contribute`), `sanction` (`PL004`, the new `post` scope) and `volume` (`PL005`, 30 an hour) -- and the precedence is **the opposite of messaging's** on purpose: an unapproved member who is also restricted hears that they are not approved, because that is the refusal that outlives the sanction, where telling them about the restriction would invite an appeal they could win and still not be able to post. Widens `sanction.scope` with `post` and both `subject_type` lists with `panel_post` -- reporting one needs no grant, because reaching the public is gated and getting help about somebody who has is not. Two things it fixes that `message` still has: `removal_is_whole` spells `removed_kind IS NOT NULL` because `NULL IN (...)` is NULL and a CHECK only fails on FALSE, and the rewrite guard permits exactly one UPDATE on a removed post -- the `ON DELETE SET NULL` cascade blanking `removed_by` -- without which deleting an account fails for anybody who ever removed a post.  `..._panel-reactions-following.sql` is T-252: `panel_reaction` (the same closed six as `message_reaction`, so a member who learned them in a conversation does not meet a different vocabulary here) and `member_follow` (two ids and a timestamp). **Neither asks `member_may_contribute`** -- reacting and following are open to any member, and a gate on them would be a second, quieter approval nobody decided to create -- and neither has anywhere to put a word, which is what keeps them from becoming posting access. A reaction to a removed post is refused (`PL007`) and the ones a post already had go with it when it is tombstoned: left behind they would be a count attached to nothing, and on a moderated post a visible record of how many people agreed with something taken down. `member_follow` is its own table rather than a `followed_entity` row because that one carries no foreign key on what it points at -- fine for teams, which are never deleted, wrong for accounts, which are -- and it is not `friendship` because a friendship is a mutual pair both sides agreed to. It shares the block with both: `PL003` in either direction, and an existing follow ends both ways when a block is created.  `..._match-panel-opening.sql` is T-253: `match_panel`, one row per fixture that has a public discussion, naming who opened it and why. **Until this migration every fixture had one**, because nothing said which did -- ten thousand rooms a season, each costing the same to moderate as one somebody wanted. Not a boolean on `fixture`: a flag has no actor and no history, and "who decided this match should have a public discussion" is the question asked after one goes wrong. Closing is not deleting -- a closed panel stays readable and takes no new posts, because taking the words down when the argument ends would rewrite a record people were told was public. `fixture_panel_open(fixture)` is the single definition, and `panel_post_a_open_guard` (`PL015`) is named to fire **before** the approval guard: a member told they are not an approved contributor would go and read about approval, and none of it would help when there is no panel here and would not be one for them if they were approved tomorrow. The boundary reports it as `MatchPanelPage.state` -- three answers, not two, because "nobody opened one" and "one is open and nobody has spoken" are different facts and only the second is something a reader can act on; `PanelPermission.refusal` gains `no_panel` and `panel_closed`, checked before anything about the viewer for the same reason the trigger fires first. The page renders them as separate sentences (`panel-none` and `panel-empty`), and `REFUSAL_TEXT` being keyed by the contract's union is what made the compiler name both new ones rather than letting a member meet a blank. The operator's half is `panel-admin.controller.ts` (`moderator` or `admin`, the pair blueprint 7.3 gives the reports queue): `GET /admin/panels?state=`, `POST /admin/fixtures/:id/panel` to open or reopen, and `POST .../panel/close`. **Not a `DELETE`, and the method is the argument** -- nothing is deleted, and a closed panel stays readable. `internal/panel-admin-store.ts` writes each decision with its `audit_log` row in one transaction, `target_type` `fixture` rather than `user_account` because the thing that changed is a match and "who opened the discussion on *this match*" is the only question the row is ever asked. Opening and reopening are one call, because they are one decision; a reopening records why it is open now rather than why it was opened a month ago, and the audit row keeps the old state beside the new. The list carries the two team names and the post count with the row -- a panel an operator cannot recognise is one they cannot decide about, and forty posts is a different decision from none.  `..._notifications.sql` is T-270: `notification` (`subject_type`/`subject_id` as the deep link, the shape `report` and `audit_log` use; `source_id` nullable, so a **block is applied at emission** -- the fastest way to undo a block is a notification saying the blocked member did something -- and `PL003` refuses one either way round), `notification_preference` (**one row per departure from the default, not one per member per kind**: writing the defaults in at registration would freeze each member's settings at the day they joined, so changing one afterwards would reach nobody, silently) and `quiet_hours` (two local `time` values read in `user_account.timezone`, because a stored UTC offset drifts by an hour twice a year and is wrong for exactly the people who set it). `in_quiet_hours(user, moment)` handles a window that **wraps midnight**, which is the ordinary case and the one a single range comparison gets exactly backwards -- quiet all day except at night. `deliver_after` is never null, so "now" and "held" differ by value rather than by nullness, and a held notification says why. A partial unique index on `dedupe_key` is how "nothing is emitted twice" is enforced rather than remembered, and it is partial so an emitter with nothing sensible to deduplicate on is not forced to invent a key. |
| `seed/*.sql` | Development fixtures, `<nnn>_<slug>.sql`, applied in prefix order. Fixed UUIDs and `ON CONFLICT (id) DO UPDATE`, so re-running converges. `001` is the catalog slice, `002` one finished fixture, `003` three API-Football ids and an honest coverage profile for the seeded season, `004` training-store aliases for the two seeded English clubs, `005` the football-data division of the two seeded leagues (an UPDATE-only seed), `006` the aliases and Persian transliterations of the seeded teams, competitions and people (T-038), `007` one scheduled match in 2099 for the prediction journey (T-080). Never product data: the runner refuses `NODE_ENV=production`. |
| `src/index.ts` | Locates and orders the migration files. |
| `src/seed.ts` | Locates and orders the seed files, and the `pnpm seed` runner: one transaction per file, rolled back whole on failure. |
| `src/migrations.spec.ts` | Enforces the naming, the unique ordering, and that every migration has a non-empty down section. |
| `src/seed.spec.ts` | Enforces seed naming and ordering, that every `INSERT` carries its `ON CONFLICT`, the production refusal, and the per-file transaction. |

Applying them needs `DATABASE_URL` and a running Postgres:

```bash
DATABASE_URL=... pnpm --filter @fmip/db migrate:up
DATABASE_URL=... pnpm --filter @fmip/db migrate:down
DATABASE_URL=... pnpm --filter @fmip/db build seed   # seed runs from dist
```

**Catalog conventions (T-010).** Every table has a `uuid` primary key that we
generate, `created_at`/`updated_at` maintained by trigger, and named
constraints. Enumerations (`competition.kind`, `team.kind`, `stage.kind`,
`player_spell.position`, …) are `text` columns under `CHECK` constraints, not
Postgres enum types (D-024). Names are never unique; the only unique natural
keys are standardised codes (`country.code`, the FIFA trigram) and structural
pairs (`season(competition_id, label)`, `stage(season_id, sort_order)`). Two
partial unique indexes carry business rules: one current season per
competition, one open `player_spell` per (person, team).

**Fixture conventions (T-011).** A team's involvement in a fixture is a row in
`fixture_participant` (one `home`, one `away`, a team at most once), and
`lineup`, `incident` and `fixture_stat` reference that row, not `team`: a
lineup for a team that is not playing cannot be written. Scores are one row
per kind in `fixture_score` (`current`, `half_time`, `full_time`,
`extra_time`, `penalties`, `aggregate`); the clock is `fixture_period` with
real start and end times. `fixture_stat` is `(metric, value)` under a closed
metric list; an absent row means *not supplied* and is never stored as zero
(rule 3). Children of a fixture cascade on delete; references into the
catalog (`team`, `person`, `season`) are `RESTRICT`.

**Ingestion conventions (T-012).** `provider_mapping(provider, external_id,
entity_type)` is unique and is the only place a provider id lives (rule 2);
`internal_id` is not a foreign key because it points at one of eight tables by
`entity_type`, so the entity resolver (T-013) checks existence on write. One
internal entity may carry several ids from one provider (upstream duplicates)
and one id per provider is the norm. `coverage_profile` is one row per
`(season, module)` in the four `CoverageState` values from
`packages/contracts`; a missing row means unknown and is read as
`not_supplied`, and a state other than `not_supplied` must name its provider.
`ingest_run` is `running` exactly while `finished_at` is null, a failed run
must carry its error, and a partial unique index allows one running row per
`(provider, job)`, which is the lock that makes a duplicate scheduler tick
harmless. Provider identifiers (`api_football`, `football_data_org`,
`highlightly`) are the D-013 bake-off set; T-025 changes them with a
constraint swap.

**Entity resolution (T-013).** `EntityResolverService.resolve(ref)` returns
`resolved` with the internal UUID, or `queued` after upserting one row in
`unresolved_entity` per `(provider, entity_type, external_id)`; a second
sighting bumps `seen_count`, never adds a row (the UNIQUE constraint, not
discipline). It never creates a catalog row. `link(ref, internalId, actor,
note)` is the only way a mapping comes to exist: it checks the target row
exists in the entity's table, inserts into `provider_mapping` with
`ON CONFLICT DO NOTHING`, and reports `conflict` if the id already points
elsewhere. The queue row is closed with actor, time, target and note (rule
10). The API-side test runs against the real schema when `DATABASE_URL` is
set and is skipped, visibly, when it is not.

**Accounts (T-040).** No secret is stored: passwords are scrypt hashes with
their parameters in the string, sessions and e-mailed tokens are HMACs of the
random value the client holds (D-026). The session is an HttpOnly, SameSite=Lax
cookie named `fmip_session`; a login always mints a new one and never promotes
a cookie the client already had, which is the session-fixation defence. Wrong
password and unknown account are the same 401 in the same time (a decoy hash
is verified when the account is unknown); forgotten-password requests always
answer 202. A password reset revokes every session. E-mail tokens are single
use, decided by one `UPDATE ... WHERE used_at IS NULL`. `username` and `email`
are unique and lower-cased. Outbound mail goes through the `MAILER` port; the
provider is chosen at deployment (T-074), so `LogMailer` prints the link,
which is what local development wants anyway.

---

## Environment variables

Every variable in `.env.example`, and what reads it. Adding one means editing
both files (`CLAUDE.md` §5). `.env` itself is never committed.

| Variable | Read by | Notes |
|---|---|---|
| `NODE_ENV` | everything | |
| `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` | `docker-compose.yml` | Configure the container at first start. Changing them after the volume exists has no effect. |
| `POSTGRES_PORT` | `docker-compose.yml` | Host port, bound to `127.0.0.1`. Default `5432`. |
| `DATABASE_URL` | `apps/api`, `packages/db`, `apps/model` | Required by the API at boot since T-013 (`src/database/database.module.ts` refuses to guess). For processes run on the host; use `127.0.0.1`, not `localhost` (psycopg on Windows tries `::1` first and waits two minutes). The `api` container does not use the `.env` value: compose derives its own from the `POSTGRES_*` values and the `postgres` service name, because `localhost` inside a container is that container. Turbo passes it through (`globalEnv`), so `pnpm test` reaches the database-backed tests. |
| `REDIS_PORT` | `docker-compose.yml` | Host port, bound to `127.0.0.1`. Default `6379`. |
| `REDIS_URL` | `apps/api` | BullMQ, and since T-231 the chat fan-out between instances. Without it the conversation still works over HTTP but nothing is delivered live, and the bus says so (`healthy: false`, one error line) rather than accepting broadcasts quietly. Same host caveat as `DATABASE_URL`. |
| `API_PORT` | `apps/api`, `docker-compose.yml` | Host port for the API. Rejected at boot if it is not a valid port number. |
| `COMPOSE_FILE` | `.env` on the VPS only | `deploy/docker-compose.prod.yml`, so every plain `docker compose` on the server means the production stack (T-074). Unset in development. |
| `SITE_HOST` | `deploy/docker-compose.prod.yml`, `deploy/Caddyfile` | The public host name Caddy serves and Cloudflare proxies; production derives `SITE_URL` and `WEB_BASE_URL` from it (T-074). |
| `HTTP_PORT`, `HTTPS_PORT` | `deploy/docker-compose.prod.yml` | Caddy’s published ports. Default 80 and 443; the laptop rehearsal uses 8080 and 8443. |
| `WEB_PORT` | `docker-compose.yml` | Host port for the web app, bound to `127.0.0.1`. Default `3000`. The container itself always listens on 3000; compose sets Next's own `PORT` for it. |
| `API_BASE_URL` | `apps/web` | Where the web app reaches the API server-side. Default `http://127.0.0.1:3001`. |
| `LOG_FORMAT` | `apps/api` | `json` (one object per line; the default when `NODE_ENV=production`) or `pretty` (the development default) — T-071. |
| `SITE_URL` | `apps/web` | The public origin of the web app: canonical URLs, language alternates, the sitemap and structured data (T-039). Default `http://localhost:3000`; set to the real origin in production. |
| `MODEL_SERVICE_URL` | `apps/api` | Where the model service answers, e.g. `http://model:8000` in compose. Internal only, never reachable from the browser. When set, the API's live contract test runs against it. |
| `BACKUP_DIR`, `BACKUP_KEEP_LOCAL_DAYS` | `scripts/backup` | Where `backup.sh` writes dumps and manifests (git-ignored, default `./backups`) and how many days of local copies it keeps (default 7). |
| `BACKUP_RCLONE_REMOTE`, `BACKUP_KEEP_REMOTE_DAYS`, `BACKUP_RCLONE_CONFIG` | `scripts/backup` | The off-provider rclone destination (a `crypt` remote, see `07-backups.md`), its retention (default 90 days) and the `rclone.conf` holding it. Unset remote = local copies only, and the script warns (D-032). |
| `MODEL_PORT` | `apps/model` | The port the model service listens on. Default `8000`. |
| `DELIVERY_EMAIL_PROVIDER`, `DELIVERY_PUSH_PROVIDER` | `apps/api` delivery (T-330) | `off` or unset is an honest absence reported by `/health/delivery` and the inbox; a name this build cannot drive stops the API at boot. No provider is known yet. |
| `INTELLIGENCE_PROVIDER`, `INTELLIGENCE_MODEL`, `INTELLIGENCE_EFFORT`, `ANTHROPIC_API_KEY` | `apps/api` intelligence (Phase 5, T-401) | `off` or unset is an honest absence that `/health/intelligence` reports; `anthropic` needs the key on the same server or the API refuses to start; the model defaults to the reference's current default and the effort to `medium`. |
| `SESSION_SECRET` | `apps/api` | Required at boot, at least 32 characters. Keys the HMAC of session and e-mail tokens (D-026); rotating it signs everyone out and voids every unused e-mail link. |
| `WEB_BASE_URL` | `apps/api` | Where the links in verification and password-reset e-mails point, and the origin allowed to open a chat socket (T-230). Default `http://localhost:3000`. |
| `CHAT_ALLOWED_ORIGINS` | `apps/api` | Extra origins allowed to open a chat socket, comma-separated. Only needed when the browser reaches the socket through an edge that is not `WEB_BASE_URL`. An access control, not a convenience: a WebSocket handshake ignores CORS, so anything named here may open a socket carrying a visitor's session (T-230, D-055). |
| `NEXT_PUBLIC_CHAT_SOCKET_URL` | `apps/web` | Where the browser opens the chat socket (T-237). Empty in every real deployment -- the edge routes `/me/conversations/socket` to the API on the same origin (T-234), which is what keeps the session cookie working with no CORS. Set it only in development, where the web app and the API are two ports with nothing in front of them. |
| `API_FOOTBALL_KEY`, `FOOTBALL_DATA_ORG_KEY`, `HIGHLIGHTLY_KEY` | `packages/ingestion` (recording script), the bake-off (T-024) | Free-tier keys, verified 2026-09-10 (`05-data-providers.md`). Sent in headers, never in URLs; recordings are scanned for them. |
| `DEMONSTRATION_DATA` | `apps/web`, `deploy/preview/start.mjs`, `scripts/public-preview.sh` (T-087, D-065) | `on` states that the football in this deployment's database is development fixture data. Every page then carries a band saying so and a marked title, nothing is indexable, the sitemap is empty and `robots.txt` forbids the site. A statement about the data, not the instruction that loaded it: `PREVIEW_SEED` is spent at boot, this stays true while the data does. The preview supervisor refuses to seed unless this is `on`, and the quick tunnel refuses to open unless it may set it: both places where fixture data stops being local. Anything but `on`, including absent, is a normal deployment. |
| `INGESTION_SOURCE`, `INGESTION_SCHEDULE`, `HIGHLIGHTLY_DAILY_BUDGET` | `apps/api` ingestion jobs (T-026, D-049) | `INGESTION_SOURCE` is `replay` (recordings, no key, the default and what CI uses), `live` (football-data.org for the spine, Highlightly for the detail) or `off`. `INGESTION_SCHEDULE=on` in the one process that should poll, nowhere else. `HIGHLIGHTLY_DAILY_BUDGET` is the ceiling the budget transport holds, 100 by default. |
| `PLAYWRIGHT_CHROMIUM_EXECUTABLE` | `apps/web/playwright.config.ts` | Tooling only, not application config, so it is deliberately **not** in `.env.example`. Points the E2E run at an already-installed browser, for an environment that cannot download one. Unset in CI. |

---

## Dependency rules

```
apps/web      →  packages/contracts, packages/ui
apps/api      →  packages/contracts, packages/ingestion, packages/db
apps/model    →  (independent; contract defined in packages/contracts)
packages/*    →  packages/config only
```

`apps/web` must **never** import from `apps/api` or `packages/db`. If a type is
needed on both sides, it belongs in `packages/contracts`.

---

## How to find things

| I need to… | Go to |
|---|---|
| Change what the API returns | `packages/contracts` first, then the module |
| Add a data provider | `packages/ingestion/adapters/` |
| Change the database shape | `packages/db/migrations/` |
| Change how a match page looks | `apps/web/app/[locale]/match/` |
| Change rating maths | `apps/api/src/modules/reputation/` |
| Change the forecast model | `apps/model/` |
| Add an environment variable | `.env.example` **and** this file |
